{
  "contract_id": "push-production-ladder-one-remote-one-local",
  "purpose": "compact production ladder proof for preflight, remote snapshot hash listing, dry-run plan upload, batched apply, journal inspect, and inspect-first recovery on one remote source site, one imported local site, and one later drift observation of the same remote identity",
  "pull_pipeline": {
    "exporter": "scans the merge base and coverage evidence",
    "importer": "persists the base package as immutable provenance",
    "persisted_base_package": {
      "base_manifest_id": "pull-2026-05-24T00:00:00Z",
      "remote_site_id": "remote-example",
      "base_coverage_hash": "sha256:pull-base-coverage"
    }
  },
  "pull_to_push_mapping": {
    "exporter": "scans the merge base and coverage evidence",
    "importer": "persists the base package as immutable provenance",
    "persisted_pull_base_package": "is immutable provenance, not a mutable cache",
    "push_preflight": "binds the persisted pull base package to one live remote identity and one short-lived push session",
    "push_snapshot_hashes": "lists the live remote comparison surface for planning only and never becomes write authority",
    "push_plan_dry_run": "uploads the canonical dry-run plan and returns an eligibility receipt, not a lock",
    "push_batch_apply": "revalidates fresh live evidence before every batch and again at the storage boundary, separate from dry-run",
    "push_journal": "records durable evidence without authorizing mutation",
    "push_recover inspect": "starts with inspect and classifies finish, rollback, retry, or block before any mutating repair",
    "push_recover auto|finish|rollback": "may mutate only after inspect proves the branch safe with the same auth floor as the write path"
  },
  "auth_and_session": {
    "required_floor": "at least as strict as current Reprint HMAC usage",
    "preflight_binding": "mints one short-lived push session bound to one remote identity and one persisted pull base",
    "mutating_calls": "dry-run, apply, and mutating recovery require the push session, canonical push signature, and idempotency key"
  },
  "push_ladder": [
    {
      "stage": "push_preflight",
      "proof": "binds the persisted pull base to the live remote identity and a short-lived push session"
    },
    {
      "stage": "push_snapshot_hashes",
      "proof": "lists the live remote comparison set for planning only"
    },
    {
      "stage": "push_plan_dry_run",
      "proof": "uploads the canonical plan as eligibility evidence and returns a receipt, not a lock"
    },
    {
      "stage": "push_batch_apply",
      "proof": "revalidates fresh live evidence before every batch and again at the storage boundary"
    },
    {
      "stage": "push_journal",
      "proof": "reads durable evidence without authorizing mutation"
    },
    {
      "stage": "push_recover inspect",
      "proof": "starts with inspect and may block when finish or rollback cannot be proven safe"
    },
    {
      "stage": "push_recover auto|finish|rollback",
      "proof": "mutates only when the journal and fresh live hashes prove the action"
    }
  ],
  "remote_liveness": {
    "remote_snapshot_hash_listing": "planning evidence only and never write authority",
    "snapshot_hash_listing": "planning evidence only and never write authority",
    "dry_run": "uploads an eligibility receipt only and never reserves remote state",
    "apply": "revalidates the live remote before every batch and again at the storage boundary",
    "journal": "reads durable evidence without authorizing mutation",
    "recovery_inspect": "must happen before any mutating recovery mode",
    "recovery_mutate": "requires fresh live hashes plus journal evidence"
  },
  "journal_and_recovery": {
    "journal_inspect": "reads durable evidence without authorizing mutation",
    "recover_inspect": "must happen before any mutating recovery mode",
    "lease_fence": "claim generation and lease expiry fence stale workers before mutation",
    "revalidation": "mutating recovery still requires fresh live hashes plus journal evidence"
  },
  "topology": {
    "networking": {
      "ingress_port": 8080,
      "proxy_policy": "local-only",
      "tunnels": "disallowed"
    },
    "docker": {
      "remote_base": "remote-base",
      "local_edited": "local-edited",
      "remote_changed": "remote-changed",
      "proof": [
        "one private network",
        "remote-base and remote-changed are the same remote identity at different times",
        "exporter/importer establish the immutable pull base package before push",
        "push preflight binds the imported base to one live remote identity and one short-lived session",
        "push snapshot hashes are planning evidence only",
        "push dry-run returns a receipt, not a lock",
        "push batch apply revalidates live evidence before every batch and at the storage boundary",
        "push journal stays read-only",
        "push recovery inspect happens before any mutating repair",
        "browser-visible inspection goes through the sandbox-provided 8080 ingress"
      ]
    },
    "playground": {
      "remote_base": "remote-base",
      "local_edited": "local-edited",
      "remote_changed": "remote-changed",
      "proof": [
        "separate disposable blueprints",
        "remote-base and remote-changed are the same remote identity at different times",
        "exporter/importer establish the immutable pull base package before push",
        "push preflight binds the imported base to one live remote identity and one short-lived session",
        "push snapshot hashes are planning evidence only",
        "push dry-run returns a receipt, not a lock",
        "push batch apply revalidates live evidence before every batch and at the storage boundary",
        "push journal stays read-only",
        "push recovery inspect happens before any mutating repair",
        "browser-visible inspection goes through the sandbox-provided 8080 ingress"
      ]
    }
  },
  "required_invariants": [
    "pull exporter/importer establish the immutable base package before push",
    "push preflight must bind the persisted pull base to one live remote identity and one short-lived session",
    "remote snapshot hash listing is planning evidence, not write authority",
    "dry-run and apply are separate remote operations",
    "apply must revalidate the live remote before every batch and at the storage boundary",
    "journal inspection is read-only and never authorizes mutation by itself",
    "recovery must begin with inspect before any mutating repair",
    "dry-run is a receipt, not a lock",
    "push batch apply is separate from dry-run",
    "push snapshot hashes are planning evidence only and never become write authority",
    "push dry-run is an eligibility receipt, not a lock",
    "authentication must be at least as strict as current Reprint HMAC usage"
  ]
}
