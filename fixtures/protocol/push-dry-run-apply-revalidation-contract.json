{
  "contract_id": "push-dry-run-apply-revalidation-contract-one-remote-one-local",
  "purpose": "ties live snapshot planning, dry-run eligibility, apply-time revalidation, and storage-boundary proof into one compact contract",
  "pull_handoff": {
    "exporter": "scans the merge base and coverage evidence",
    "importer": "persists the base package as immutable provenance",
    "preflight": "binds the persisted pull base to the live remote identity and a short-lived push session",
    "snapshot_hashes": "lists live remote hashes for planning only and never acts as write authority",
    "dry_run": "uploads the canonical plan as eligibility evidence and returns a receipt, not a lock",
    "apply": "revalidates fresh live evidence before every batch and again at the storage boundary"
  },
  "plan_bindings": {
    "base_manifest_id": "pull-2026-05-24T00:00:00Z",
    "push_session": "psh_01j00000000000000000000000",
    "snapshot_id": "snap_01j00000000000000000000000",
    "coverage_hash": "sha256:remote-coverage",
    "plan_id": "plan_2026-05-24T00:00:00Z_001",
    "dry_run_id": "dry_01j00000000000000000000000"
  },
  "apply_revalidation": {
    "before_each_batch": "fresh live hashes",
    "at_storage_boundary": "filesystem-compare-rename and mysql-transaction-row-lock",
    "rejected_if": [
      "the snapshot listing is stale",
      "the remote changed after the dry-run receipt",
      "the storage boundary can no longer prove the staged write"
    ]
  },
  "journal_and_recovery": {
    "journal_cursor": "journal:dry_01j00000000000000000000000:2",
    "inspect_mode": "inspect",
    "mutating_modes": [
      "auto",
      "finish",
      "rollback"
    ],
    "inspect_proof": "journal-and-live-hashes-reviewed",
    "inspect_is_read_only": true
  },
  "topology": {
    "remote_base": "remote-base",
    "local_edited": "local-edited",
    "remote_changed": "remote-changed",
    "runner": "runner",
    "same_remote_identity": true,
    "browser_ingress_port": 8080,
    "proxy_policy": "local-only",
    "tunnels": "disallowed"
  },
  "required_invariants": [
    "dry-run and apply are separate remote operations",
    "the dry-run receipt never becomes a lock",
    "apply must revalidate the live remote before every batch and at the storage boundary",
    "the remote may drift between dry-run and apply",
    "inspect is read-only and must happen before any mutating recovery",
    "auth must be at least as strict as current Reprint HMAC usage"
  ]
}
