{
  "contract_id": "push-protocol-extension-production-contract",
  "purpose": "compact end-to-end proof for preflight, remote snapshot hash listing, dry-run plan upload, batched apply, journal inspect, and inspect-first recovery with explicit pull provenance mapping, apply-time revalidation, and one-remote-one-local topology",
  "pull_pipeline": {
    "exporter": "scans the merge base and coverage evidence",
    "importer": "persists the base package as immutable provenance",
    "persisted_base_package": {
      "base_manifest_id": "pull-2026-05-24T00:00:00Z",
      "base_manifest_hash": "sha256:pull-base-manifest",
      "base_coverage_hash": "sha256:pull-base-coverage",
      "remote_site_id": "remote-example"
    }
  },
  "production_boundary": {
    "preflight": "first live binding after importer provenance exists",
    "remote_snapshot_hash_listing": "planning evidence only and never write authority",
    "dry_run_plan_upload": "uploads the canonical plan as an eligibility receipt, not a lock",
    "mutation_batch_apply": "revalidates fresh live evidence before every batch and again at the storage boundary, separate from dry-run",
    "journal_inspect": "reads durable evidence without authorizing mutation",
    "recovery_inspect": "starts with inspect and classifies finish, rollback, retry, or block before any mutating repair",
    "recovery_mutation": "may mutate only when journal evidence plus fresh live hashes prove the action safe"
  },
  "push_sequence": [
    "push_preflight",
    "push_snapshot_hashes",
    "push_plan_dry_run",
    "push_batch_apply",
    "push_journal",
    "push_recover inspect",
    "push_recover auto|finish|rollback"
  ],
  "push_phases": {
    "preflight": "binds the persisted pull base package to one live remote identity and one short-lived push session",
    "snapshot_hash_listing": "reads the live remote comparison surface for planning only and never becomes write authority",
    "dry_run_upload": "uploads the canonical plan as an eligibility receipt, not a lock",
    "batch_apply": "revalidates fresh live evidence before every batch and at the storage boundary, and remains separate from dry-run",
    "journal_inspect": "reads durable claim, lease, fencing, and apply-boundary evidence without authorizing mutation",
    "recovery_inspect": "runs before any mutating recovery branch and classifies finish, rollback, retry, or block",
    "recovery_mutate": "may mutate only after inspect proves the branch safe with fresh live evidence and the same auth floor as the write path"
  },
  "pull_to_push_mapping": {
    "exporter": "scans the merge base and coverage evidence",
    "importer": "persists the base package as immutable provenance",
    "push_preflight": "binds the persisted pull base package to one live remote identity and one short-lived push session",
    "push_snapshot_hashes": "reads the live remote comparison surface for planning only and never becomes write authority",
    "push_plan_dry_run": "uploads the canonical plan as an eligibility receipt, not a lock",
    "push_batch_apply": "revalidates fresh live evidence before every batch and again at the storage boundary, separate from dry-run",
    "push_journal": "reads durable evidence without authorizing mutation",
    "push_recover inspect": "starts with inspect and classifies finish, rollback, retry, or block before any mutating repair",
    "push_recover auto|finish|rollback": "may mutate only when journal evidence plus fresh live hashes prove the action safe"
  },
  "push_guards": {
    "snapshot_hash_listing": "returns live remote comparison evidence for planning only and never upgrades into write authority",
    "dry_run_receipt": "proves eligibility only and cannot be reused as a liveness lock",
    "apply_revalidation": "refreshes live evidence before every batch and again at the storage boundary",
    "journal_inspect": "reads durable claim and fencing evidence without mutating target resources",
    "recovery_inspect": "must happen before any mutating recovery mode and may block when finish or rollback cannot be proven safe",
    "remote_liveness": "dry-run and apply are separate remote operations",
    "dry_run": "eligibility evidence only and never a lock",
    "apply": "must revalidate the live remote before every batch and at the storage boundary",
    "journal": "read-only evidence only",
    "recovery": "inspect-first and mutates only when journal evidence plus fresh live hashes prove the action",
    "auth_floor": "at least as strict as current Reprint HMAC usage"
  },
  "bridge_rule": "the importer-owned base package is immutable provenance for planning, apply, journal, and recovery",
  "auth_session_recovery_proofs": {
    "auth_session_fencing": "fixtures/protocol/push-auth-session-fencing-contract.json",
    "auth_session_recovery": "fixtures/protocol/push-auth-session-recovery-contract.json",
    "recovery_inspect": "fixtures/protocol/push-recovery-inspect-contract.json"
  },
  "topology": {
    "remote_base": "remote-base",
    "local_edited": "local-edited",
    "remote_changed": "remote-changed",
    "runner": "runner",
    "same_remote_identity": true,
    "networking": {
      "ingress_port": 8080,
      "proxy_policy": "local-only",
      "tunnels": "disallowed"
    },
    "proof": [
      "remote-base and remote-changed are the same remote identity observed at different times",
      "local-edited carries the imported local edits used to build the canonical plan",
      "runner owns preflight, remote snapshot hash listing, dry-run plan upload, batch apply, journal inspect, and recovery",
      "remote snapshot hash listing stays planning-only and never upgrades into write authority",
      "pull exporter/importer establish the immutable base package before push",
      "remote-base and remote-changed are the same remote identity observed at different times",
      "local-edited carries the imported local edits used to build the canonical plan",
      "dry-run is a receipt, not a lock",
      "apply revalidates fresh live evidence before every batch and at the storage boundary",
      "journal inspect is read-only",
      "recovery inspect happens before any mutating repair",
      "Docker and Playground both keep the same remote identity mapping and route names",
      "browser-visible inspection stays on the sandbox-provided 8080 ingress through a local-only proxy"
    ],
    "pull_to_push_bridge": [
      "exporter/importer establish the immutable base package before push",
      "preflight is the first live binding after importer persistence",
      "snapshot hash listing stays planning-only",
      "dry-run is an eligibility receipt, not a lock",
      "apply revalidates fresh live evidence before every batch and at the storage boundary",
      "journal inspect stays read-only",
      "recovery starts with inspect before any mutating repair"
    ],
    "docker": {
      "network": "one private network",
      "proof": [
        "remote-base seeds the persisted pull base",
        "remote-changed reuses the same remote identity after drift",
        "remote snapshot hash listing stays planning-only",
        "dry-run and apply remain separate remote operations",
        "apply revalidates fresh live evidence before every batch and at the storage boundary"
      ]
    },
    "playground": {
      "network": "separate disposable blueprints",
      "proof": [
        "remote-base and remote-changed are the same remote identity at different times",
        "push preflight, dry-run, apply, journal, and recovery use the same route names as Docker",
        "remote snapshot hash listing stays planning-only",
        "dry-run and apply remain separate remote operations",
        "browser-visible inspection stays on the sandbox-provided 8080 ingress through a local-only proxy"
      ]
    }
  },
  "lab_topology": {
    "remote_base": {
      "role": "remote source site",
      "identity": "remote-example",
      "produces": "persisted pull base package"
    },
    "local_edited": {
      "role": "imported local site",
      "identity": "local-dev-site",
      "has_user_edits": true
    },
    "remote_changed": {
      "role": "same remote source site later in time",
      "identity": "remote-example",
      "drift_witness": true
    },
    "remote_identity": "remote-example",
    "local_identity": "local-dev-site",
    "live_drift": {
      "between": ["remote_base", "remote_changed"],
      "proof": [
        "remote-base and remote-changed are the same remote identity observed at different times",
        "dry-run and apply remain separate remote calls",
        "apply revalidates fresh live evidence before every batch and again at the storage boundary",
        "recovery inspect happens before any mutating repair"
      ]
    }
  },
  "required_invariants": [
    "dry-run and apply are separate remote operations",
    "remote snapshot hash listing is planning evidence, not write authority",
    "dry-run is a receipt, not a lock",
    "apply must revalidate the live remote before every batch and at the storage boundary",
    "journal inspection is read-only and never authorizes mutation by itself",
    "recovery must begin with inspect before any mutating repair",
    "authentication must be at least as strict as current Reprint HMAC usage",
    "preflight binds the persisted pull base package to one live remote identity and one short-lived push session",
    "pull exporter/importer establish the immutable base package before push",
    "one remote source site, one imported local site, and one drift witness are enough to prove the production topology",
    "remote snapshot hash listing may page large sites but never becomes write authority",
    "dry-run and apply stay separate even when the same runner executes both",
    "recovery inspect stays read-only and classifies finish, rollback, retry, or block before any mutating repair",
    "the pull exporter/importer pipeline remains the only source of immutable push provenance",
    "browser-visible inspection stays on the sandbox-provided 8080 ingress through a local-only proxy"
  ]
}
