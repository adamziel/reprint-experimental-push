# Supervisor Decision

- Time: 2026-05-25 09:59:20 CEST
- Status: blocked; no release-state change in this pass.
- Going well: the visible surface stays compact and the checked gate remains explicit.
- Not going well: `reliable-executor` still lacks a real live URL/topology, and `integration` still has not run the real-site release command.
- Progress change: none.
- Next nudge: `integration` owns the next real-site release command; `reliable-executor` is drifting until the gate binds to a real endpoint, and `invariants`/`recovery` should align to that same command.
- Evidence needed to move: a retained live-source/auth/recovery run on a real endpoint.
