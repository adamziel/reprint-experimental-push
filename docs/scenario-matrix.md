# Scenario Matrix

The first executable matrix lives in `test/push-planner.test.js`.

The planner policy summary lives in
[No Overwrite Playbook](invariants/no-overwrite-playbook.md).
The compact boundary note lives in
[No Overwrite Boundaries](invariants/no-overwrite-boundaries.md).
The shortest no-data-loss summary lives in
[No Data Loss Invariants](invariants/no-data-loss-invariants.md).
The plain-language invariant note lives in
[No Data Loss Invariants](invariants/no-data-loss-invariants.md).

| Scenario | Expected behavior | Current evidence |
| --- | --- | --- |
| Remote site is unchanged since pull | Local file and row changes are planned and applied. | `plans and applies local changes when remote still matches the pull base` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves matching independent edits | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves matching independent edits` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit and remote-only plugin drift | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit and remote-only plugin drift` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent edit and a remote-only descendant | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit stays `already-in-sync`, the remote-only descendant stays preserved, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent edit and a remote-only descendant` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent file type swap and refuses late drift | The plan stays ready, the delete keeps its live remote precondition, the matching file type swap stays `already-in-sync`, and a late remote change is refused without disturbing the remote-only plugin drift. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent file type swap and refuses late drift` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching plugin-owned resource | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching plugin-owned resource` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching plugin-owned resource and same-remote graph identity | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource and same-remote graph identity stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and same-remote graph identity` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves late plugin-owned drift | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned drift stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` even when the remote mutates the same plugin-owned value after the plan is computed. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves late plugin-owned drift` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete, edit, type swap, and plugin-owned resource and refuses late plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete, edit, type swap, and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete, edit, type swap, and plugin-owned resource and refuses late plugin drift on re-apply` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan refuses late drift and preserves remote-only plugin removal evidence | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit stays `already-in-sync`, and late drift is refused on re-apply without disturbing unrelated remote-only plugin removal evidence. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan refuses late drift and preserves remote-only plugin removal evidence` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a remote-only plugin removal and refuses late plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the remote-only plugin removal stays `keep-remote`, and late plugin drift is refused on re-apply after the remote removal is preserved. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a remote-only plugin removal and refuses late plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan and a matching plugin-owned resource share live preconditions | The plan stays ready, the delete and matching plugin-owned resource each keep a live remote precondition, the plugin-owned resource stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a delete and plugin-owned resource share live preconditions` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, and the remote-only plugin removals stay `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removal evidence, then refuses late plugin-owned drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin removal evidence stays `keep-remote`, and late plugin-owned drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource, remote-only plugin removal evidence, and refuses late plugin-owned drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit and remote-only plugin removals and refuses late removal drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit stays `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late removal drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves matching independent edit and remote-only plugin removals and refuses late removal drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete and a matching plugin-owned resource and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late remote-only plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete and a matching plugin-owned resource and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit, a matching plugin-owned resource, and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching edit and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit, a matching plugin-owned resource, and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift after remote-only plugin removal | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late plugin-owned drift is refused on re-apply after the remote removal is preserved. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift on re-apply after remote-only plugin removal` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, and late plugin-owned drift is refused on re-apply without disturbing the preserved graph identity. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves remote-only plugin removals and refuses late removal drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the remote-only plugin removals stay `keep-remote`, and late removal drift is refused on re-apply after the remote removal is preserved. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves remote-only plugin removals and refuses late removal drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals after apply revalidation | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late plugin-owned drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals after apply revalidation` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete, matching plugin-owned resource, and remote-only plugin removals and refuses late plugin-owned drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete and plugin-owned resource stay `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late plugin-owned drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete, matching plugin-owned resource, and remote-only plugin removals and refuses late plugin-owned drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete and a matching plugin-owned resource and refuses late remote-only plugin drift after apply revalidation | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late remote-only plugin drift is refused after apply revalidation. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete and a matching plugin-owned resource and refuses late remote-only plugin drift after apply revalidation` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete and remote-only plugin drift and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete and remote-only plugin drift and refuses late drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent row delete, a matching plugin-owned resource, and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching row delete and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late remote-only plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent row delete, a matching plugin-owned resource, and refuses late remote-only plugin drift on re-apply` |
| Release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late remote-only plugin drift | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late drift is refused on re-apply. | `keeps same-remote graph identity at the release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late remote-only plugin drift` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late remote-only plugin drift is refused on re-apply while unrelated remote-only plugin drift stays preserved. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit and refuses late plugin-owned drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin-owned drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit and refuses late plugin-owned drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift without disturbing remote-only plugin drift | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin-owned drift is refused on re-apply without disturbing unrelated remote-only plugin drift. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift without disturbing remote-only plugin drift` |
| Live release boundary keeps remote-only plugin removals while a ready delete plan preserves a matching plugin-owned resource and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and a late plugin-owned drift is refused on re-apply. | `keeps remote-only plugin removals at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late drift on re-apply` |
| Live release boundary keeps remote-only plugin removals while a ready delete plan preserves a matching independent edit and file type swap and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late drift is refused on re-apply. | `keeps remote-only plugin removals at the live release boundary while a ready delete plan preserves a matching independent edit, matching independent file type swap, and refuses late drift on re-apply` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent edit and a matching plugin-owned resource and refuses late plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent edit and a matching plugin-owned resource and refuses late plugin drift on re-apply` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent edit, matching plugin-owned resource, and matching file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching edit, plugin-owned resource, and file type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent edit, matching plugin-owned resource, and a matching file type swap` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent delete, matching independent edit, and matching file type swap and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete, edit, and file type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent delete, matching independent edit, and matching file type swap and refuses late drift on re-apply` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent delete, matching independent edit, and matching file type swap and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete, edit, and file type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves matching independent delete, matching independent edit, and matching independent file type swap and refuses late drift on re-apply` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves same-remote graph identity and refuses late plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, same-remote graph identity stays intact, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves same-remote graph identity and refuses late plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit, matching plugin-owned resource, and remote-only plugin drift | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit and plugin-owned resource stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit, matching plugin-owned resource, and remote-only plugin drift` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit and a matching plugin-owned resource, then refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit and a matching plugin-owned resource, then refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit and plugin-owned resource and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit and plugin-owned resource and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves bounded live evidence for matching independent edit, file type swap, and plugin-owned resource | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit, file type swap, and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and the recorded evidence stays limited to the mutation id, resource key, and live remote hash. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves bounded live evidence for matching independent edit, file type swap, and plugin-owned resource` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete, edit, and file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching delete, edit, and file type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves matching independent delete, edit, and file type swap` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete, edit, file type swap, and plugin-owned resource | The plan stays ready, the delete keeps its live remote precondition, the matching delete, edit, file type swap, and plugin-owned resource stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete, edit, file type swap, and plugin-owned resource` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent edit, file type swap, and row edit | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit, file type swap, and row edit stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote` while late plugin drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent edit, matching independent file type swap, and matching independent row edit` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent row delete and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent row delete stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent row delete and refuses late drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent row delete and remote-only plugin drift | The plan stays ready, the delete keeps its live remote precondition, the matching independent row delete stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent row delete and remote-only plugin drift` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, and the remote-only plugin removals stay `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals` |
| Live release boundary refuses a forged ready delete when its live remote precondition is stripped | The executor rejects the forged delete with `PRECONDITION_FAILED`, the matching independent edit stays `already-in-sync`, and the remote-only plugin drift stays untouched. | `refuses a forged ready delete at the live release boundary while remote-only plugin drift stays untouched` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves same-remote graph identity, a matching restore, and a matching independent file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching restore, graph identity, and file type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves same-remote graph identity, a matching restore, and a matching independent file type swap` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves same-remote graph identity and a matching plugin-owned resource | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` while unrelated plugin drift remains untouched. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves same-remote graph identity and a matching plugin-owned resource` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves same-remote graph identity and late plugin-owned drift | The plan stays ready, the delete keeps its live remote precondition, the matching row and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and a late plugin-owned drift remains preserved on re-apply. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves same-remote graph identity and late plugin-owned drift` |
| Live release boundary keeps late plugin-owned drift while a ready delete plan preserves same-remote graph identity and a matching plugin-owned resource | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, and a late plugin-owned drift on the live remote is left untouched alongside unrelated remote-only plugin drift. | `keeps late plugin-owned drift at the live release boundary while preserving a ready delete precondition and remote-only plugin drift` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves remote-only plugin removals and refuses late drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, same-remote graph identity stays intact, the remote-only plugin removal stays `keep-remote`, and a late remote plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves remote-only plugin removals and refuses late drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin-owned drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late drift on either the plugin-owned row or the remote plugin record is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and remote-only plugin removals` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late remote-only plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves late remote-only plugin drift after planning | The plan stays ready, the delete keeps its live remote precondition, the matching independent edit stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and a late remote-only plugin change stays preserved on apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves late remote-only plugin drift after planning` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift without disturbing remote-only plugin drift | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin-owned drift is refused on re-apply without disturbing unrelated remote-only plugin drift. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource and refuses late plugin-owned drift without disturbing remote-only plugin drift` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete and refuses late remote-only plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late plugin drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete and refuses late remote-only plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent delete and remote-only plugin removals | The plan stays ready, the delete keeps its live remote precondition, the matching independent delete stays `already-in-sync`, and the remote-only plugin removals stay `keep-remote`. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent delete and remote-only plugin removals` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching plugin-owned resource, a matching file type swap, and remote-only plugin removals | The plan stays ready, the delete keeps its live remote precondition, the matching plugin-owned resource and file type swap stay `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching plugin-owned resource, a matching independent file type swap, and remote-only plugin removals` |
| Live release boundary keeps remote-only plugin changes while a ready delete plan preserves a matching independent row delete and refuses late plugin drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching independent row delete stays `already-in-sync`, the remote-only plugin changes stay `keep-remote`, and late plugin drift is refused on re-apply. | `keeps remote-only plugin changes at the live release boundary while a ready delete plan preserves a matching independent row delete and refuses late plugin drift on re-apply` |
| Live release boundary keeps same-remote graph identity while a ready delete plan preserves a matching independent edit and a matching plugin-owned resource and refuses late remote drift on re-apply | The plan stays ready, the delete keeps its live remote precondition, the matching edit and plugin-owned resource stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and late remote drift is refused on re-apply. | `keeps same-remote graph identity at the live release boundary while a ready delete plan preserves a matching independent edit and a matching plugin-owned resource and refuses late remote drift on re-apply` |
| Live release boundary refuses a forged plugin-owned delete when its live remote precondition is stripped | The executor rejects the forged plugin-owned delete with `PRECONDITION_FAILED`, the matching independent row stays unchanged, and the remote-only plugin drift stays untouched. | `rejects a forged ready plugin-owned delete when the live remote precondition is stripped at the release boundary` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching independent file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching file type swap stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching independent file type swap` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching restore and same-remote graph identity | The plan stays ready, the delete keeps its live remote precondition, the matching restore stays `already-in-sync`, same-remote graph identity stays intact, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching restore and same-remote graph identity` |
| Live release boundary keeps remote-only plugin drift while a ready delete plan preserves a matching restore, edit, and file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching restore, edit, and file type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching restore, edit, and file type swap` |
| Live release boundary keeps remote-only plugin removals while a ready delete plan preserves a matching independent file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching file type swap stays `already-in-sync`, and the remote-only plugin removal stays `keep-remote`. | `keeps remote-only plugin removals at the live release boundary while a ready delete plan preserves a matching independent file type swap` |
| Live release boundary keeps remote-only plugin removals while a ready delete plan preserves same-remote graph identity and a matching independent file type swap | The plan stays ready, the delete keeps its live remote precondition, the matching file type swap stays `already-in-sync`, and the remote-only plugin removals stay `keep-remote`. | `keeps remote-only plugin removals at the live release boundary while a ready delete plan preserves same-remote graph identity and a matching independent file type swap` |
| Forged ready plan omits a live remote precondition | Executor rejects the plan with `PRECONDITION_FAILED`; no mutation is applied. | `executor rejects forged ready plans missing live remote preconditions` |
| Forged ready delete plan omits a live remote precondition | Executor rejects the delete with `PRECONDITION_FAILED`; no mutation is applied. | `executor rejects forged ready delete plans missing live remote preconditions` |
| Forged ready file type swap plan omits a live remote precondition | Executor rejects the type swap with `PRECONDITION_FAILED`; no mutation is applied. | `executor rejects forged ready file type swap plans missing live remote preconditions` |
| Forged mixed ready plan omits one live remote precondition | Executor rejects the whole plan with `PRECONDITION_FAILED`; remote-only plugin drift remains untouched and no mutation is applied. | `executor rejects forged mixed ready plans when any live remote precondition is missing` |
| Forged mixed ready delete and type swap plan omits the delete precondition | Executor rejects the whole plan with `PRECONDITION_FAILED`; the matching type swap is not allowed to proceed alone, remote state stays unchanged, and the missing precondition evidence is bounded. | `executor rejects forged mixed ready delete and type swap plans when the delete precondition is missing` |
| Live remote drifts after a ready delete plan was computed | Executor refuses the plan with `PRECONDITION_FAILED`; the late remote change is preserved and unrelated plugin drift stays untouched. | `executor refuses a ready delete plan when the live remote changes after dry run while unrelated plugin drift is preserved` |
| Forged ready plan points a precondition at the wrong mutation or resource | Executor rejects the plan with `PRECONDITION_FAILED`; no mutation is applied. | `executor rejects forged ready plans with mismatched live remote preconditions` |
| Planner keeps every live mutation behind a live remote precondition while remote-only plugin drift and matching independent edits are present | The plan stays bounded: every mutation carries a live-remote precondition, the matching edit stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `planner keeps every live mutation behind a live remote precondition while preserving remote-only plugin drift and matching independent edits` |
| Remote changed, local did not | Remote change is kept; no mutation is produced. | `keeps remote-only changes and does not overwrite them` |
| Remote and local changed different resources | Local mutations apply while remote-only changes are preserved. | `combines non-overlapping local and remote changes` |
| Local deleted a file or row, remote still matches pull base | Delete mutations are planned only with matching live remote preconditions; no stale delete is emitted without a live-remote hash check. | `plans local deletions only behind live remote preconditions` |
| Local directory delete would hide a live remote descendant while remote-only plugin drift exists elsewhere | The directory delete is the only refused mutation, the descendant evidence stays bounded, the remote plugin drift stays `keep-remote`, and no mutation is emitted. | `blocks a directory delete that would hide a live remote descendant while preserving remote-only plugin drift` |
| Local delete, matching edit, and file type swap while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and file type swap stay safe with apply verification` |
| Local row delete, matching edit, and file type swap while remote-only plugin removals changed elsewhere | The row delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned row delete, matching independent edit, and matching file type swap stay safe with apply verification` |
| Local file delete would hide a live remote descendant while matching independent edit and create stay safe and remote-only plugin drift exists elsewhere | The file delete is refused with bounded topology evidence, the matching edit and create stay `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `blocks a file delete that would hide a live remote descendant while preserving matching independent edit, type swap, and remote-only plugin drift` |
| Local file delete would hide a live remote descendant while a matching independent edit and remote-only plugin drift exist elsewhere | The file delete is refused with bounded topology evidence, the matching edit stays `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `blocks a file delete that would hide a live remote descendant while preserving a matching edit and remote-only plugin drift with bounded evidence` |
| Local file delete would hide a live remote type change while a matching independent type swap and remote-only plugin drift exist elsewhere | The file delete is refused with bounded topology evidence, the matching type swap stays `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `stops a local file delete when the remote turned the same file into a directory while preserving a matching independent type swap and remote-only plugin drift` |
| Local file delete, matching restore, edit, and file type swap while remote-only plugin drift exists elsewhere, then remote drift appears after apply | The delete is the only mutation and stays behind a live remote precondition; the matching restore, edit, and type swap stay `already-in-sync`; the remote plugin drift stays `keep-remote`; and the executor refuses the stale plan on re-apply after late drift. | `keeps remote-only plugin drift at the live release boundary while a ready delete plan preserves a matching restore, edit, and file type swap and refuses late drift on re-apply` |
| Forged ready delete plan strips its live precondition after planning while remote-only plugin drift exists elsewhere | The executor refuses the forged plan with `PRECONDITION_FAILED`, no mutation is applied, and the remote-only plugin drift stays untouched. | `refuses a forged ready delete plan when its live precondition is stripped after planning` |
| Local file delete, matching independent row restore, and matching file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition; the matching row restore and type swap stay `already-in-sync`; and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete preserves a matching independent row restore and file type swap` |
| Local file type swap, matching restore, and independent row edit while remote-only plugin removals exist elsewhere | The type swap stays `already-in-sync`, the matching restore and row edit stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned file type swap preserves a matching restore and independent row edit` |
| Local file delete, matching independent restore, and matching edit while remote-only plugin removals exist elsewhere | The delete is the only mutation and stays behind a live remote precondition; the matching restore and edit stay `already-in-sync`; and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned file delete preserves a matching independent restore and edit` |
| Local file delete, matching independent restore, edit, and type swap while remote-only plugin drift and removals exist elsewhere | The delete is the only mutation and stays behind a live remote precondition; the matching restore, edit, and type swap stay `already-in-sync`; and the remote plugin drift and removals stay `keep-remote`. | `keeps remote-only plugin drift and removals while a live-preconditioned file delete preserves a matching independent restore, edit, and type swap` |
| Local file type swap, matching independent edit, and matching restore while remote-only plugin drift exists elsewhere | The file type swap is the only mutation and stays behind a live remote precondition; the matching edit and restore stay `already-in-sync`; and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file type swap preserves a matching independent edit and restore` |
| Local file delete would hide a live remote type change while a matching independent type swap, matching edit, and remote-only plugin drift exist elsewhere | The file delete is refused with bounded topology evidence, the matching type swap and edit stay `already-in-sync`, the remote plugin drift stays `keep-remote`, and no mutation is emitted for the unsafe path. | `stops a local file delete when the remote independently type swaps the same path while preserving a matching independent type swap, matching edit, and remote-only plugin drift with bounded evidence` |
| Local file type swap would hide a live remote descendant while matching independent edit stays safe and the remote side removed an unrelated plugin | The type swap is refused with bounded topology evidence, the matching edit stays `already-in-sync`, the removed plugin stays `keep-remote`, and no mutation is emitted. | `blocks a file type swap that would hide a live remote descendant while preserving a matching independent edit and remote-only plugin removal with bounded evidence` |
| Local file delete would hide a live remote descendant while matching independent edit and independent row delete stay safe and the remote side removed an unrelated plugin | The delete is refused with bounded topology evidence, the matching edit and row delete stay `already-in-sync`, the removed plugin stays `keep-remote`, and no mutation is emitted. | `stops a file delete that would hide a live remote descendant while preserving a matching independent edit, row delete, and remote-only plugin removal` |
| Local file delete would hide a live remote descendant while matching independent delete, edit, type swap, and remote-only plugin drift stay safe | The delete is refused with bounded descendant evidence, the matching delete, edit, and type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a file delete that would hide a live remote descendant while preserving a matching independent delete, edit, type swap, and remote-only plugin drift` |
| Local delete, matching independent edit, and file type swap while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local delete, matching independent delete, edit, and file type swap while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching delete, edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while matching independent delete, edit, and file type swap stay already in sync beside a live-preconditioned file delete` |
| Local delete, matching independent edit, and file type swap while remote-only plugin removals changed elsewhere, with apply verification | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local delete, matching row restore, matching edit, and file type swap while remote-only plugin changes exist elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching row restore, edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row restore, edit, and file type swap stay safe with apply verification` |
| Local delete, matching restore, matching edit, and file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore, edit, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, edit, and file type swap stay safe` |
| Local file delete, matching file delete, matching edit, and file type swap while remote-only plugin drift exists elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching file delete, edit, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin drift while a live-preconditioned file delete preserves a matching independent file delete, edit, and type swap` |
| Plugin-owned delete is blocked after the owner plugin was removed remotely while matching independent delete, edit, type swap, and remote-only plugin drift stay safe | The plugin-owned delete is refused with bounded owner-context evidence, the matching delete, edit, and type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and no mutation is applied. | `blocks plugin-owned deletes when the owner plugin was removed remotely while preserving matching independent delete, edit, type swap, and remote-only plugin drift with bounded evidence` |
| Plugin-owned delete is blocked when ownership metadata is missing while matching independent delete, edit, type swap, and remote-only plugin drift stay safe | The plugin-owned delete is refused with bounded evidence, the matching type swap and row edits stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and no mutation is applied. | `blocks a plugin-owned delete without ownership metadata while preserving matching independent delete, edit, type swap, and remote-only plugin drift` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent edit, and matching independent type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and matching independent type swap stay safe with apply verification` |
| Remote-only plugin change while a ready delete plan preserves a matching independent edit, matching independent file type swap, and remote-only plugin removals | The plan stays ready, the delete keeps its live remote precondition, the matching edit and file type swap stay `already-in-sync`, the remote-only plugin removals stay `keep-remote`, and late plugin drift is refused on re-apply. | `keeps remote-only plugin changes at the live release boundary while a ready delete plan preserves a matching independent edit, matching independent file type swap, and remote-only plugin removals` |
| Remote-only plugin change while a live-preconditioned file delete and file type swap preserve a matching independent edit and still require live preconditions | The file delete and type swap stay behind live remote preconditions, the matching independent edit stays `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and file type swap preserve a matching independent edit and still require live preconditions` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent file type swap, and matching independent row delete stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent type swap and row delete stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent file type swap, and matching independent row delete stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file type swap and matching independent edit stay safe with apply verification | The file type swap is the only mutation and stays behind a live remote precondition, the matching independent edit stays `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file type swap and matching independent edit stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent edit, matching independent type swap, and matching independent row delete stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit, type swap, and row delete stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, type swap, and row delete stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent row delete preserve a matching independent edit and file type swap | The file delete and row delete are the only mutations and stay behind live remote preconditions, the matching edit and file type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent row delete preserve a matching independent edit and file type swap` |
| Remote-only plugin removal while a live-preconditioned file delete, matching independent edit, and matching file type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin removal stays `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned file delete, matching independent edit, and matching file type swap stay safe with apply verification` |
| Remote-only plugin removals while a live-preconditioned file delete, matching independent edit, and file type swap stay safe with apply verification | The file delete and file type swap stay behind live remote preconditions, the matching edit stays `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned file delete, matching independent edit, and file type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent delete, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching delete, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent delete, edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a local directory delete, matching descendant delete, and matching independent edit stay safe | The directory delete and descendant delete both stay behind live remote preconditions, the matching edit stays `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a local directory delete, matching descendant delete, and matching independent edit stay safe` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent row delete, matching independent edit, and matching independent type swap stay safe with apply verification | The file delete and row delete stay behind live remote preconditions, the matching edit and type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a local file delete, matching independent row delete, matching independent edit, and matching independent type swap stay safe` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent row delete, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching row delete, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent row delete, edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent delete, and matching independent type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching delete and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent delete, and matching independent type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent edit, and matching independent file type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and matching independent file type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching type swap and row edit stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent create, edit, restore, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching create, edit, restore, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent create, edit, restore, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent create stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching create stays `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent create stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent delete, restore, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent delete, restore, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent delete, restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while live-preconditioned file and row deletes preserve a matching independent edit, type swap, and row edit | The file and row deletes are the only mutations and stay behind live remote preconditions, the matching edit, type swap, and row edit stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while live-preconditioned file and row deletes preserve matching independent edit and type swap` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent restore, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching restore, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned row delete, matching independent file delete, restore, edit, and type swap stay safe with apply verification | The row delete is the only mutation and stays behind a live remote precondition, the matching file delete, restore, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent file delete, restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned row delete, matching independent row delete, edit, and type swap stay safe with apply verification | The row delete is the only mutation and stays behind a live remote precondition, the matching row delete, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row delete, edit, and type swap stay safe with apply verification` |
| Remote-only plugin removals while a live-preconditioned row delete, matching independent restore, edit, and type swap stay safe with apply verification | The row delete is the only mutation and stays behind a live remote precondition, the matching restore, edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned row delete, matching independent restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin removals while a plugin-owned delete and matching independent edit stay safe with apply verification | The plugin-owned delete is the only mutation and stays behind a live remote precondition, the matching edit stays `already-in-sync`, and the removed plugin resource stays `already-in-sync` while the removed plugin stays preserved through apply verification. | `applies plugin-owned deletes when remote-only plugin removals are present alongside a matching independent edit` |
| Remote-only plugin change while a live-preconditioned row delete, matching independent file delete, edit, and type swap stay safe with apply verification | The row delete is the only mutation and stays behind a live remote precondition, the matching file delete, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent file delete, edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent row delete, restore, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching row delete, restore, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row delete, restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin removal while a live-preconditioned file delete, matching independent restore, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching restore, edit, and type swap stay `already-in-sync`, and the remote plugin removal stays `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned file delete, matching independent restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin removals while a live-preconditioned file delete and matching independent restore stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching restore stays `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned file delete and matching independent restore stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent file edit, and matching independent type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent file edit and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent file edit, and matching independent type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent file edit, row edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching file edit, row edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent file edit, row edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned row delete, matching independent row restore, edit, and file type swap stay safe with apply verification | The row delete is the only mutation and stays behind a live remote precondition, the matching row restore, edit, and file type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row restore, edit, and file type swap stay safe with apply verification` |
| Plugin-owned delete is missing driver metadata while matching independent deletes and type swaps already match the live remote | The planner blocks the plugin-owned delete with bounded evidence, preserves the matching delete and type swap as `already-in-sync`, keeps remote-only plugin drift untouched, and keeps serialized evidence redacted. | `blocks plugin-owned deletes with missing driver metadata while preserving matching independent deletes, type swaps, and remote-only plugin drift` |
| Plugin-owned delete is missing driver metadata while matching independent deletes, edits, type swaps, and remote-only plugin drift are present | The planner blocks the plugin-owned delete with bounded evidence, preserves the matching delete, edit, and type swap as `already-in-sync`, and keeps remote-only plugin drift untouched. | `blocks plugin-owned deletes with missing driver metadata while preserving matching independent deletes, edits, type swaps, and remote-only plugin drift` |
| Plugin-owned delete is missing driver metadata while matching independent delete, restore, and remote-only plugin drift are present | The planner blocks the plugin-owned delete with bounded evidence, preserves the matching delete and restore as `already-in-sync`, keeps remote-only plugin drift untouched, and refuses to guess ownership. | `blocks plugin-owned deletes with missing driver metadata while preserving matching independent delete, restore, and remote-only plugin drift` |
| Plugin-owned delete is missing ownership metadata while matching independent delete, edit, type swap, and remote-only plugin drift are present | The planner blocks the plugin-owned delete with bounded evidence, preserves the matching delete, edit, and type swap as `already-in-sync`, keeps remote-only plugin drift untouched, and refuses to guess ownership. | `blocks a plugin-owned delete without ownership metadata while preserving matching independent delete, edit, type swap, and remote-only plugin drift` |
| Plugin-owned delete is missing driver metadata while a matching restore and remote-only plugin drift are present | The planner blocks the plugin-owned delete with bounded evidence, preserves the matching restore as `already-in-sync`, and keeps remote-only plugin drift untouched. | `stops plugin-owned deletes with missing driver metadata while preserving a matching restore and remote-only plugin drift` |
| Plugin-owned delete is missing driver metadata while matching independent deletes, edits, type swaps, and remote-only plugin drift already diverged on the owned row | Plan is `blocked`; the planner preserves the matching independent delete, edit, and type swap as `already-in-sync`, keeps remote-only plugin drift untouched, and redacts the blocker evidence. | `blocks plugin-owned deletes with missing driver metadata while preserving matching independent deletes, edits, type swaps, and remote-only plugin drift` |
| Stale plugin-owned delete while a matching restore and remote-only plugin drift are present | Plan is `blocked`; the planner preserves the matching restore as `already-in-sync`, keeps remote-only plugin drift untouched, and redacts the stale-owner evidence. | `blocks a stale plugin-owned delete while preserving a matching restore and remote-only plugin drift` |
| Stale plugin-owned delete while matching independent delete, edit, type swap, and remote-only plugin removals are present | Plan is `blocked`; the planner preserves the matching delete, edit, and type swap as `already-in-sync`, keeps remote-only plugin removals untouched, and redacts the stale-owner evidence. | `blocks plugin-owned deletes when the owner plugin was removed remotely while preserving matching independent delete, edit, type swap, and remote-only plugin removals` |
| Local delete, matching independent edit, and file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local directory delete would hide a live remote descendant while matching independent delete, edit, type swap, and remote-only plugin drift exist elsewhere | The planner stops the directory delete as `file-topology-conflict`, preserves the matching delete, edit, and type swap as `already-in-sync`, and keeps remote-only plugin drift out of the conflict payload. | `blocks a directory delete that would hide a live remote descendant while preserving matching independent delete, edit, type swap, and remote-only plugin drift` |
| Local directory delete would hide a live remote descendant while matching independent edit and remote-only plugin drift exist elsewhere | The planner stops the directory delete as `file-topology-conflict`, preserves the matching edit as `already-in-sync`, keeps remote-only plugin drift out of the conflict payload, and emits no mutation. | `bounds directory delete conflict evidence while preserving matching independent edit and remote-only plugin drift` |
| Local delete, matching independent edit, and matching independent file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching independent file type swap stay safe` |
| Local file delete would hide a live remote descendant while a matching delete, matching edit, and remote-only plugin drift exist elsewhere | The topology-sensitive delete stops as `file-topology-conflict`, the matching delete and edit stay `already-in-sync`, and the remote-only plugin drift stays preserved. | `stops a local file delete that would hide a live remote descendant while preserving a matching delete, matching edit, and remote-only plugin drift` |
| Local delete, matching independent file delete, and file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent file delete and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent file delete, and file type swap stay safe` |
| Local file and row deletes plus matching edit and file type swap while remote-only plugin drift exists elsewhere | Both deletes are the only mutations and stay behind live remote preconditions, the matching edit and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while live-preconditioned file and row deletes plus matching edit and file type swap stay safe` |
| Local file and row deletes while a matching independent type swap and remote-only plugin drift exist elsewhere | Both deletes are the only mutations and stay behind live remote preconditions, the matching type swap and row edit stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin drift while live-preconditioned file and row deletes coexist with a matching independent type swap` |
| Local file delete, matching independent edit, and matching file type swap while remote-only plugin drift exists elsewhere | The file delete and type swap are the only mutations, both stay behind live remote preconditions, the matching edit stays `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and matching file type swap stay safe with apply verification` |
| Local file delete, matching independent delete, edit, and file type swap while remote-only plugin drift exists elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent delete, edit, and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent delete, edit, and file type swap stay safe with apply verification` |
| Local file delete, matching independent edit, matching independent type swap, and matching independent delete while remote-only plugin drift exists elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit, delete, and type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, matching independent type swap, and matching independent delete stay safe with apply verification` |
| Local file type swap would hide a live remote descendant while matching independent delete and edit stay safe and remote-only plugin drift exists elsewhere | The type swap is refused as `file-topology-conflict`, the matching delete and edit stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote` with bounded evidence. | `blocks a file type swap that would hide a live remote descendant while preserving matching independent delete, edit, and remote-only plugin drift` |
| Local delete and matching independent restore while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore stays `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent restore stay safe with apply verification` |
| Local delete, matching independent restore, and file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin drift while a live-preconditioned delete, matching independent restore, and file type swap stay safe` |
| Local row delete, matching independent row restore, and file type swap while remote-only plugin drift exists elsewhere | The row delete is the only mutation and stays behind a live remote precondition, the matching row restore and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row restore, and matching independent file type swap stay safe` |
| Local delete, matching independent edit and delete, and file type swap while remote-only plugin drift exists elsewhere | The delete and file type swap are the only mutations and stay behind live remote preconditions, the matching independent edit and delete stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit and delete, and file type swap stay safe with apply verification` |
| Local delete, matching independent row delete, and file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching row delete and type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent row delete, and file type swap stay safe with apply verification` |
| Local delete, matching independent restore, and file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and type swap stay safe` |
| Local delete and file type swap while a matching independent edit and remote-only plugin changes exist elsewhere | Both mutations stay behind live remote preconditions, the matching edit stays `already-in-sync`, and apply preserves the remote-only plugin changes. | `keeps remote-only plugin changes while live-preconditioned delete and file type swap preserve a matching independent edit` |
| Local delete and file type swap while matching independent edit and deletion land on the same hashes and remote-only plugin drift exists elsewhere | The delete and type swap are the only mutations and stay behind live remote preconditions, the matching independent edit and deletion stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin drift while a live-preconditioned delete and file type swap preserve matching independent edits and deletions` |
| Local file delete while matching independent row delete, restore, edit, and file type swap land on the same hashes and remote-only plugin drift exists elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching row delete, restore, edit, and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent row delete, restore, edit, and type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching independent file type swap while remote-only plugin removals happen elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and apply preserves the remote-only plugin removals. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and matching independent file type swap stay safe` |
| Local delete, matching independent delete, edit, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent delete, edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent delete, edit, and file type swap stay safe` |
| Local delete, matching independent delete, edit, and file type swap while remote-only plugin changes elsewhere, with apply verification | The delete is the only mutation and stays behind a live remote precondition, the matching independent delete, edit, and type swap stay `already-in-sync`, and apply preserves the remote plugin changes. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent delete, and matching independent type swap stay safe` |
| Local delete, matching independent delete, restore, and file type swap while remote-only plugin removals happen elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching delete, restore, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent delete, restore, and file type swap stay safe` |
| Local and remote independently delete the same file while remote-only plugin drift exists elsewhere | The matching independent delete stays `already-in-sync`, no delete mutation is produced, and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while recognizing a matching independent delete` |
| Local delete, matching independent edit, matching independent delete, and file type swap while remote-only plugin removals happen elsewhere, with apply verification | The delete is the only mutation and stays behind a live remote precondition, the matching edit, delete, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, delete, and type swap stay safe with apply verification` |
| Local delete plus matching independent edit and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete plus matching independent edit and type swap stay safe with apply verification` |
| Local delete and file type swap while matching independent edit applies and remote-only plugin drift exists elsewhere, with apply verification | The delete and file type swap are the only mutations and stay behind live remote preconditions, the matching independent edit stays `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin drift while a live-preconditioned delete and file type swap preserve matching independent edits with apply verification` |
| Local delete and matching independent edit while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and matching row edit stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent edit stay safe with apply verification` |
| Local delete, matching independent edit, and matching independent restore while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit and restore stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching independent restore stay safe with apply verification` |
| Local delete, matching independent edit, matching independent delete, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit, matching delete, and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, matching independent delete, and file type swap stay safe with apply verification` |
| Local delete, matching independent row restore, edit, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching row restore, edit, and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent row restore, edit, and type swap stay safe` |
| Local delete and matching independent delete while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent delete stays `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent delete stay safe` |
| Local file delete matches the remote file delete while remote-only plugin drift exists elsewhere | The matching delete stays `already-in-sync`, the remote plugin drift stays `keep-remote`, and no mutation or precondition is emitted. | `keeps remote-only plugin changes while a matching file delete stays already-in-sync` |
| Local delete, matching independent edit, and matching file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching file type swap while remote-only plugin changes elsewhere with apply verification | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and apply preserves the remote plugin changes. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching independent file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching independent file type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching independent file type swap while remote-only plugin drift exists elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching independent file type swap stay safe` |
| Local file delete, matching independent edit, matching independent file type swap, and matching independent row edit while remote-only plugin drift exists elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit, file type swap, and row edit stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and matching independent file type swap stay safe with apply verification` |
| Matching independent plugin-owned update while remote-only plugin drift exists elsewhere | No mutation is produced for the plugin-owned row update, the planner records it as `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a matching independent plugin-owned update stays in sync` |
| Plugin-owned delete while matching independent edit and file type swap already match the live remote, with remote-only plugin drift elsewhere | Plan is `blocked`; the plugin-owned delete stays blocked by stale owner-context evidence, the matching independent edit and file type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `blocks a plugin-owned delete while preserving matching independent edit, file type swap, and remote-only plugin drift` |
| Local file delete, matching independent row edit, and file type swap while remote-only plugin changes elsewhere | The file delete and file type swap are the only mutations and stay behind live remote preconditions, the matching row edit stays `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row edit, and file type swap stay safe with apply verification` |
| Local unrelated file edit while a plugin-owned row stays already in sync and remote-only plugin metadata/files drift elsewhere | The unrelated file edit is the only mutation and keeps a live remote precondition, the plugin-owned row stays `already-in-sync`, and the remote plugin metadata/files stay `keep-remote` through apply. | `keeps remote-only plugin metadata and file drift while a plugin-owned row stays already in sync` |
| Local plugin-owned delete while the owner context independently matches and remote-only plugin drift exists elsewhere | The plugin-owned delete is the only mutation and keeps a live remote precondition, the matching owner context stays `already-in-sync`, and the unrelated remote-only plugin drift stays `keep-remote` through apply. | `applies plugin-owned deletes when the owner context independently matches and remote-only plugin drift is preserved` |
| Local plugin-owned delete while a matching independent edit, file type swap, and remote-only plugin drift exist elsewhere | Plan is `blocked`; the matching independent edit and file type swap stay `already-in-sync`, the unrelated remote-only plugin drift stays `keep-remote`, and the stale owner-context evidence stays bounded. | `blocks plugin-owned deletes when a matching independent edit, file type swap, and remote-only plugin drift are present` |
| Local plugin-owned delete while a matching independent edit and remote-only plugin drift exist elsewhere | The planner must stop on stale plugin-owner context, keep the matching independent edit `already-in-sync`, and preserve the remote-only plugin drift without applying the owned delete. | `blocks plugin-owned deletes when the owner plugin context drifted while preserving a matching independent edit` |
| Local directory delete that would hide a live remote descendant while matching independent edits and remote-only plugin drift exist elsewhere | The delete must stop as a `file-topology-conflict`, the matching independent edit stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` without leaking the descendant bytes. | `blocks a directory delete that would hide a live remote descendant while preserving matching independent edits and remote-only plugin drift` |
| Local directory delete that would hide a live remote descendant while a matching independent delete and remote-only plugin drift exist elsewhere | The delete must stop as a `file-topology-conflict`, the matching independent delete stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` without leaking descendant bytes. | `blocks a directory delete that would hide a live remote descendant while preserving matching independent delete and remote-only plugin drift` |
| Local file type swap that would hide a live remote descendant while matching independent edits and remote-only plugin drift exist elsewhere | The type swap must stop as a `file-topology-conflict`, the matching independent edit stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` without leaking descendant bytes. | `blocks a file type swap that would hide a live remote descendant while preserving remote-only plugin drift` |
| Local file type swap that would hide a live remote descendant while a matching independent delete and remote-only plugin drift exist elsewhere | The type swap must stop as a `file-topology-conflict`, the matching independent delete stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` without leaking descendant bytes. | `blocks a file type swap that would hide a live remote descendant while preserving a matching delete and remote-only plugin drift` |
| Local file type swap that would hide a live remote descendant while a matching delete, matching edit, and remote-only plugin drift exist elsewhere | The type swap must stop as a `file-topology-conflict`, the matching delete and edit stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote` without leaking descendant bytes. | `blocks a file type swap that would hide a live remote descendant while preserving matching independent delete, edit, and remote-only plugin drift` |
| Local file type swap that would hide a live remote descendant while a matching independent delete, edit, restore, and remote-only plugin drift exist elsewhere | The type swap must stop as a `file-topology-conflict`, the matching delete, edit, and restore stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and the safe delete mutation stays in the plan behind a live precondition. | `blocks a file type swap that would hide a live remote descendant while preserving a matching independent delete, edit, restore, and remote-only plugin drift` |
| Local delete and matching independent restore while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore stays `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent restore stay safe with apply verification` |
| Local delete, matching independent restore, and matching file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and matching type swap stay safe with apply verification` |
| Local delete, matching independent delete, restore, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching delete, restore, and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent delete, restore, and type swap stay safe with apply verification` |
| Local file delete, matching independent delete, edit, and file type swap while remote-only plugin removals exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching delete, edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned file delete, matching independent delete, edit, and type swap stay safe with apply verification` |
| Local delete, matching independent edit, and file type swap while remote-only plugin removals happen elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Local file type swap would hide a live remote descendant while a matching independent edit and remote-only plugin removal exist elsewhere | Plan is `conflict`; the file type swap is removed, the matching independent edit stays `already-in-sync`, the remote plugin removal stays `keep-remote`, and conflict evidence stays bounded to the file-topology relation. | `blocks a file type swap that would hide a live remote descendant while preserving a matching independent edit and remote-only plugin removal` |
| Local file type swap would hide a live remote descendant while a matching independent edit and remote-only plugin removal exist elsewhere, with apply refusal | Plan is `conflict`; the file type swap is removed, the matching independent edit stays `already-in-sync`, the remote plugin removal stays `keep-remote`, and apply refuses the plan without exposing descendant bytes. | `blocks a file type swap that would hide a live remote descendant while preserving a matching independent edit and remote-only plugin removal` |
| Live-preconditioned delete plus matching independent create and edit while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching create and edit stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent create stay safe with apply verification` |
| Local delete, matching independent type swap, and matching independent delete while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching type swap and delete stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent type swap, and matching independent delete stay safe with apply verification` |
| Local delete plus matching independent edits and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching file edit, row edit, and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent edits plus file type swap stay safe with apply verification` |
| Local delete and matching independent edits while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent file and row edits stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent edits stay safe with apply verification` |
| Live-preconditioned row delete plus matching independent edit and file type swap while remote-only plugin changes elsewhere | The row delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local file delete while matching independent file and row edits land on the same hashes and remote-only plugin changes exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching file and row edits stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent file and row edits stay safe with apply verification` |
| Local delete, matching independent edit, matching file type swap, and matching file delete while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit, file type swap, and file deletion stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Local delete, matching independent file deletion, matching independent edit, and matching file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching file deletion, edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent file deletion, matching edit, and matching type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Local delete, matching independent restore, and matching type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and matching type swap stay safe with apply verification` |
| Local delete, matching independent restore, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and file type swap stay safe with apply verification` |
| Matching independent file and row deletes, restores, edits, and type swaps while remote-only plugin drift exists elsewhere | No mutation is produced for the converged file and row changes, they stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while matching independent file and row deletes, restores, edits, and type swaps already match the live remote` |
| Local file type swap would hide a live remote descendant while matching independent delete, restore, and edit and remote-only plugin drift exist elsewhere | The file type swap is the only refused mutation, the matching delete, restore, and edit stay `already-in-sync`, the remote plugin drift stays `keep-remote`, and the planner keeps bounded evidence while stopping on the hidden descendant. | `stops a file type swap that would hide a live remote descendant while preserving matching delete, restore, edit, and remote-only plugin drift` |
| Local file type swap would hide a live remote descendant while matching independent delete, restore, edit, and remote-only plugin drift exist elsewhere, with apply refusal | The file type swap is the only refused mutation, the matching delete, restore, and edit stay `already-in-sync`, the remote plugin drift stays `keep-remote`, and the planner keeps bounded evidence while stopping on the hidden descendant. | `stops a file type swap that would hide a live remote descendant while preserving matching delete, restore, edit, and remote-only plugin drift with apply refusal` |
| Local delete, matching independent restore, matching independent delete, and matching independent file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore, delete, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, matching independent delete, and matching independent file type swap stay safe` |
| Local delete, matching independent delete, restore, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching delete, restore, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent delete, restore, and type swap stay safe with apply verification` |
| Local delete, matching independent edit, matching row edit, and type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit, matching row edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, matching row edit, and type swap stay safe with apply verification` |
| Local delete, matching independent edit, matching independent delete, and type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit, delete, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, matching independent delete, and type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching type swap while remote-only plugin removals happened elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local delete, matching independent delete, edit, and file type swap while remote-only plugin removals happened elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching delete, edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent delete, edit, and file type swap stay safe with apply verification` |
| Local delete, matching independent edit, and matching type swap while remote-only plugin changes happened elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin metadata/files stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Local delete conflicts with a remote same-file edit while remote-only plugin drift exists elsewhere | The same-file conflict stops with bounded evidence, the unrelated remote-only plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a local file delete conflict while preserving unrelated remote-only plugin drift and bounded evidence` |
| Local file delete conflicts with a remote same-file edit while remote-only plugin drift exists elsewhere | The same-file conflict stops with bounded evidence, the unrelated remote-only plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a local file delete conflict while preserving matching remote-only plugin drift` |
| Local file delete conflicts with a remote same-file edit while matching independent edit, type swap, and remote-only plugin change exist elsewhere | The same-file conflict stops with bounded evidence, the matching independent edit and type swap stay `already-in-sync`, the remote-only plugin change stays `keep-remote`, and no mutation is emitted. | `stops a local file delete conflict while preserving matching independent edit, type swap, and remote-only plugin change` |
| Local file delete conflicts with a remote same-file edit while remote-only plugin removals and matching independent delete, restore, edit, and type swap exist elsewhere | The same-file conflict stops with bounded evidence, the matching independent delete, restore, edit, and type swap stay `already-in-sync`, the remote plugin removals stay `keep-remote`, and no mutation is emitted. | `stops a local file delete conflict while preserving remote-only plugin removals and matching independent delete, restore, edit, and type swap` |
| Local row delete conflicts with a remote same-row update while matching independent file type swap and remote-only plugin drift exist elsewhere | The row conflict stops with bounded evidence, the matching file type swap stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a local row deletion when the remote independently updated the same row while preserving a matching independent file type swap and remote-only plugin drift` |
| Local file delete would hide a live remote descendant while remote-only plugin drift exists elsewhere | The delete is blocked with bounded descendant evidence, the unrelated remote-only plugin drift stays `keep-remote`, and no mutation is emitted. | `blocks a file delete that would hide a live remote descendant while preserving remote-only plugin drift` |
| Local file delete would hide a live remote descendant while matching independent delete, edit, type swap, and remote-only plugin drift exist elsewhere | The delete is blocked with bounded descendant evidence, the matching delete, edit, and type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and no mutation is emitted. | `blocks a file delete that would hide a live remote descendant while preserving matching independent delete, edit, type swap, and remote-only plugin drift` |
| Local delete and matching independent edit while remote-only plugin changes elsewhere, with apply verification | The delete and matching independent edit are the only mutations, both stay behind live remote preconditions, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent edit apply safely` |
| Unsupported plugin-owned option update while matching independent delete, edit, type swap, and remote-only plugin removal land elsewhere | The plugin-owned update is blocked, the matching independent delete, edit, and type swap stay `already-in-sync`, the remote-only plugin removal stays `keep-remote`, and the blocker evidence stays bounded and redacted. | `blocks unsupported plugin-owned option updates while preserving matching independent delete, edit, type swap, and remote-only plugin removal` |
| Unknown plugin-owned custom table row while matching independent edit and type swap land on the same hashes and remote-only plugin drift exists elsewhere | The unsupported row is blocked, the matching independent edit and type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and the blocker evidence stays bounded and redacted. | `blocks unknown plugin-owned custom table rows while preserving matching independent edits and remote-only plugin drift` |
| Local file delete, matching independent edit, and file type swap while remote-only plugin changes exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local file delete that would hide a live remote descendant while matching independent edit and remote-only plugin drift exist | The file delete is blocked, the matching independent edit stays `already-in-sync`, and the remote plugin drift stays `keep-remote` while the conflict evidence remains bounded. | `blocks a file delete that would hide a live remote descendant while preserving matching independent edits and remote-only plugin drift` |
| Local file delete that would hide a live remote descendant while matching independent delete, restore, edit, and remote-only plugin drift exist elsewhere | The file delete stops as a `file-topology-conflict`, the matching delete, restore, and edit stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote` with bounded evidence. | `blocks a file delete that would hide a live remote descendant while preserving matching independent delete, restore, edit, and remote-only plugin drift` |
| Local file type swap conflicts with a remote descendant while remote-only plugin drift exists elsewhere | The type swap stays blocked with bounded conflict evidence, the unrelated remote plugin drift stays `keep-remote`, and no mutation is emitted. | `bounds file type swap conflict evidence while preserving unrelated remote-only plugin drift` |
| Local file delete and matching independent file deletion while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent file deletion and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent file deletion, and type swap stay safe with apply verification` |
| Local file delete while a matching independent row deletion, edit, and file type swap land on the same hashes and remote-only plugin changes exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching row deletion, edit, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row deletion, edit, and type swap stay safe with apply verification` |
| Local file delete while a matching independent delete, edit, and file type swap land on the same hashes and remote-only plugin changes exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching delete, edit, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent delete, matching edit, and type swap stay safe with apply verification` |
| Local file delete, matching independent delete, matching edit, and type swap while remote-only plugin changes elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent delete, edit, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent delete, matching edit, and type swap stay safe with apply verification` |
| Local file delete, matching independent edit, matching independent row delete, and file type swap while remote-only plugin changes elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching edit, matching row delete, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, matching independent row deletion, and file type swap stay safe with apply verification` |
| Local file delete, matching independent edit, matching independent row delete, and file type swap while remote-only plugin removals exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching edit, matching row delete, and type swap stay `already-in-sync`, and the remote plugin removal stays `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, matching independent row deletion, and file type swap stay safe with apply verification` |
| Local file delete, matching independent edit, matching row edit, and file type swap while remote-only plugin removals changed elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching edit, row edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, matching row edit, and type swap stay safe with apply verification` |
| Local file delete, matching independent edit, and file type swap while remote-only plugin changes elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local file delete, matching independent edit, matching row delete, and file type swap while remote-only plugin changes elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit, row delete, and file type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent edit, row delete, and type swap stay safe with apply verification` |
| Local file delete, matching independent edit, and file type swap while remote-only plugin changes elsewhere, with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local directory delete would hide a live remote descendant while unrelated remote-only plugin drift exists | The directory delete stays blocked with bounded topology evidence, the unrelated remote plugin drift stays `keep-remote`, and no mutation is emitted. | `blocks a directory delete that would hide a live remote descendant while preserving unrelated plugin drift evidence` |
| Local file type swap with a descendant delete that drifted remotely while remote-only plugin drift exists | The type swap stays blocked as a file conflict with bounded evidence, the unrelated remote plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a file type swap when the descendant delete drifts remotely` |
| Local directory delete with a descendant delete that drifted remotely while remote-only plugin drift exists | The descendant delete stays blocked as a file conflict with bounded evidence, the parent directory delete is not emitted, the unrelated remote plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a directory delete when the descendant delete drifts remotely` |
| Matching independent delete while remote-only plugin drift changed elsewhere | The delete stays `already-in-sync`, remote-only plugin drift stays `keep-remote`, and apply preserves the remote plugin changes without creating a mutation. | `keeps remote-only plugin changes while a matching independent deletion stays safe with apply verification` |
| Live-preconditioned file delete, matching independent edit, and file type swap while remote-only plugin changes elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and file type swap stay safe with apply verification` |
| Live-preconditioned file delete, matching independent edit, and file type swap while remote-only plugin removals changed elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned file delete, matching independent edit, and type swap stay safe with apply verification` |
| Local file delete, matching independent restore, edit, and file type swap while remote-only plugin removals changed elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching restore, edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent restore, edit, and type swap stay safe` |
| Local row delete, matching independent row delete, matching edit, and type swap while remote-only plugin changes elsewhere | The row delete is the only mutation and stays behind a live remote precondition, the matching independent row delete, edit, and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row deletion, edit, and type swap stay safe with apply verification` |
| Local deletion while remote-only plugin files or metadata changed elsewhere | Ready plan still preserves the remote plugin changes and keeps the local deletion behind a live remote precondition. | `keeps remote-only plugin changes while allowing an unrelated local deletion` |
| Local delete, matching edit, and file type swap while remote-only plugin files or metadata changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching edit, and type swap stay safe` |
| Local delete, matching independent deletion, edit, and file type swap while remote-only plugin files or metadata changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent deletion, edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent deletion, edit, and type swap stay safe with apply verification` |
| Local delete, matching independent restore, and file type swap while remote-only plugin files or metadata changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and matching type swap stay safe with apply verification` |
| Local delete, matching file edit, matching row edit, and file type swap while remote-only plugin changes elsewhere | The delete and type swap are the only mutations, both stay behind live remote preconditions, the matching file and row edits stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned deletion, matching file edit, matching row edit, and type swap stay safe` |
| Local delete, matching independent file edit, matching row edit, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching file edit, row edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching file edit, matching row edit, and type swap stay safe with apply verification` |
| Local file delete with a matching independent restore while remote-only plugin files or metadata changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore stays `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent restore stay safe with apply verification` |
| Local file delete, matching independent edit, restore, and file type swap while remote-only plugin files or metadata changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit, restore, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, restore, and type swap stay safe with apply verification` |
| Local file delete and matching independent restore while remote-only plugin removals changed elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching restore stays `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a local file delete and matching independent restore stay safe` |
| Local delete, matching restore, and file type swap while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching restore, and type swap stay safe with apply verification` |
| Local delete, matching restore, and file type swap while remote-only plugin removals exist elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching restore, and type swap stay safe with apply verification` |
| Local delete, matching independent restore, matching independent edit, and file type swap while remote-only plugin changes exist elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching restore, edit, and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, matching independent edit, and type swap stay safe with apply verification` |
| Local matching independent deletion, edit, and file type swap while remote-only plugin removals changed elsewhere | The deletion, edit, and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a matching independent deletion, edit, and type swap stay safe with apply verification` |
| Plugin-owned deletions are blocked even when unrelated matching independent deletions, edits, and type swaps are safe | The plugin-owned delete stays `blocked`, the matching independent delete, edit, and type swap stay `already-in-sync`, the remote plugin drift stays `keep-remote`, and apply refuses the stale plugin-owned mutation. | `blocks plugin-owned deletions while preserving remote-only plugin drift and matching independent deletions, edits, and type swaps` |
| Local directory deletion and matching descendant deletion while remote-only plugin files or metadata changed elsewhere | Both deletions are allowed behind live remote preconditions, the remote plugin changes stay `keep-remote`, and apply removes only the targeted directory subtree. | `keeps remote-only plugin changes while a local directory delete and matching descendant delete stay safe` |
| Local deleted a file while remote still matched base | The delete is planned as a mutation with a live remote precondition and apply removes only the targeted file. | `plans local file deletions only behind live remote preconditions` |
| Local deleted a row, remote edited the same row | Plan is `conflict`; conflict evidence reports delete-vs-update without row contents; apply refuses. | `stops a local deletion when the remote edited the same resource` |
| Local deleted a file, remote edited the same file | Plan is `conflict`; conflict evidence reports delete-vs-update without file contents; apply refuses. | `stops a local file deletion when the remote edited the same file` |
| Local deleted a file, remote edited the same file, and remote-only plugin drift changed elsewhere | Plan is `conflict`; conflict evidence stays redacted, the remote-only plugin drift stays `keep-remote`, and apply refuses. | `bounds file delete conflict evidence while preserving unrelated remote-only plugin drift` |
| Local deleted a file, remote edited the same file, matching independent edits exist, and remote-only plugin drift changed elsewhere | Plan is `conflict`; the matching independent edit stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, conflict evidence stays redacted, and apply refuses. | `stops a local file deletion conflict while preserving matching independent edits and remote-only plugin drift` |
| Local deleted a file, remote edited the same file, and remote-only plugin drift changed elsewhere while matching delete, restore, edit, and type swap stay safe | Plan is `conflict`; the matching delete, restore, edit, and type swap stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, conflict evidence stays redacted, and apply refuses. | `stops a file delete conflict while preserving remote-only plugin drift and matching independent delete, restore, edit, and type swap` |
| Local deleted a file, remote turned the same file into a directory | Plan is `conflict`; conflict evidence reports delete-vs-type-change without revealing directory contents; apply refuses. | `stops a local file deletion when the remote turned the same file into a directory` |
| Local deleted a plugin-owned option, remote removed the owning plugin | Plan is `blocked`; plugin-owner-context evidence names the plugin and its file context without option payloads; apply refuses. | `stops a plugin-owned option deletion when the owner plugin was removed remotely` |
| Local updated a plugin-owned option, remote removed the owning plugin, and the plan only declares that plugin as a dependency | Plan is `blocked`; stale plugin-owner-context evidence names the plugin without leaking option payloads, and apply refuses. | `stops a plugin-owned option update when the owner plugin was removed remotely` |
| Local deleted a plugin-owned option while remote-only plugin files or metadata changed elsewhere | Plan is `blocked`; the remote plugin drift stays `keep-remote`, blocker evidence stays redacted, and apply refuses the stale delete. | `stops a plugin-owned option deletion while preserving unrelated remote-only plugin drift` |
| Local deleted a plugin-owned option while remote-only plugin files or metadata changed elsewhere and matching independent edits stay in sync | Plan is `blocked`; the remote plugin drift stays `keep-remote`, the matching independent file edit and row edit stay `already-in-sync`, blocker evidence stays redacted, and apply refuses the stale delete. | `blocks plugin-owned option deletions while preserving remote-only plugin drift and matching independent edits` |
| Local deleted a plugin-owned option while remote-only plugin files or metadata changed elsewhere and matching independent restore and edit stay in sync | Plan is `blocked`; the remote plugin drift stays `keep-remote`, the matching independent file restore and row edit stay `already-in-sync`, blocker evidence stays redacted, and apply refuses the stale delete. | `blocks plugin-owned deletions while preserving remote-only plugin drift and matching independent restore and edit` |
| Local deleted a plugin-owned option while remote-only plugin files or metadata changed elsewhere and matching independent deletions stay in sync | Plan is `blocked`; the remote plugin drift stays `keep-remote`, the matching independent file delete, row delete, and type swap stay `already-in-sync`, blocker evidence stays redacted, and apply refuses the stale delete. | `blocks plugin-owned option deletions while preserving remote-only plugin drift and matching independent deletions` |
| Local deleted a plugin-owned option while remote-only plugin files or metadata changed elsewhere and matching independent deletions, edits, and type swaps stay in sync | Plan is `blocked`; the remote plugin drift stays `keep-remote`, the matching independent file delete, row delete, edit, and type swap stay `already-in-sync`, blocker evidence stays redacted, and apply refuses the stale delete. | `blocks plugin-owned deletes while preserving matching independent deletions, edits, type swaps, and remote-only plugin drift` |
| Local deleted a plugin-owned option while the owner plugin was removed remotely and matching independent delete, edit, and type swap stay in sync | Plan is `blocked`; the stale owner context wins, the matching independent delete, edit, and type swap stay `already-in-sync`, blocker evidence stays redacted, and apply refuses. | `blocks plugin-owned deletes when the owner plugin was removed remotely while preserving matching independent delete, edit, and type swap` |
| Unsupported plugin-owned option update while unrelated remote-only plugin drift changed elsewhere | Plan is `blocked`; the unsupported plugin-owned mutation stays blocked, the remote plugin drift stays `keep-remote`, and the blocker evidence stays bounded. | `blocks unsupported plugin-owned option updates while preserving unrelated remote-only plugin drift` |
| Local deleted a plugin-owned option while remote-only plugin files or metadata changed elsewhere, matching independent deletions stay in sync, and the plan still stays blocked | Plan is `blocked`; the remote plugin drift stays `keep-remote`, the matching independent file delete, file type swap, and row edit stay `already-in-sync`, blocker evidence stays redacted, and apply refuses the stale delete. | `blocks plugin-owned deletions while preserving remote-only plugin drift and matching independent deletions` |
| Unsupported plugin-owned option update while matching independent delete, edit, type swap, and remote-only plugin drift are present | Plan is `blocked`; the unsupported plugin-owned mutation stays blocked, the matching independent delete, edit, and type swap stay `already-in-sync`, the remote plugin drift stays `keep-remote`, and the blocker evidence stays bounded. | `blocks unsupported plugin-owned option updates while preserving matching independent delete, edit, type swap, and remote-only plugin drift` |
| Plugin-owned row with declared ownership but missing driver metadata | Plan is `blocked`; the blocker reports `missing-plugin-driver`, remote-only plugin drift stays `keep-remote`, and the blocker evidence stays bounded without leaking the local payload. | `blocks plugin-owned rows with missing driver metadata while preserving remote-only plugin drift` |
| Live-preconditioned delete, matching row restore, and file type swap while remote-only plugin changes elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching row restore and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching row restore, and type swap stay safe` |
| Live-preconditioned delete, matching restore, and file type swap while remote-only plugin changes elsewhere, with apply verification | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, the remote plugin change stays `keep-remote`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching restore, and type swap stay safe with apply verification` |
| Live-preconditioned delete, matching independent edit, and file type swap while remote-only plugin drift changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and type swap stay safe with apply verification` |
| Live-preconditioned delete, matching independent edit, and file type swap while remote-only plugin drift changed elsewhere, with apply verification | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Live-preconditioned file delete and matching independent edits while remote-only plugin drift changed elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching row edit and matching option edit stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent edits stay safe with apply verification` |
| Local file delete, matching independent edit, and file type swap while remote-only plugin changes exist elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and file type swap stay safe with apply verification` |
| Local directory delete and matching descendant delete while remote-only plugin drift exists elsewhere | Both deletes stay behind live remote preconditions, the descendant delete is allowed because it is also deleted locally, the remote plugin drift stays `keep-remote`, and the remote tree is removed without loss. | `allows a live-preconditioned directory delete when the live descendant is also deleted locally and remote-only plugin drift is preserved` |
| Live-preconditioned delete, matching independent row edit, and file type swap while remote-only plugin drift changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching row edit and type swap stay `already-in-sync`, and the remote plugin drift stays `keep-remote` through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent row edit, and type swap stay safe with apply verification` |
| Live-preconditioned delete, matching independent edit, and file type swap while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and type swap stay safe` |
| Live-preconditioned file and row deletes plus matching edit and type swap while remote-only plugin removals changed elsewhere | The file and row deletes are the only mutations and stay behind live remote preconditions, the matching edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while live-preconditioned file and row deletes plus matching edit and type swap stay safe` |
| Live-preconditioned delete and matching independent edit while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit stays `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply. | `keeps remote-only plugin removals while a live-preconditioned delete and matching independent edit stay safe` |
| Live-preconditioned delete, matching independent edit, and matching file type swap while remote-only plugin removals changed elsewhere | The delete is the only mutation and stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and matching file type swap stay safe with apply verification` |
| Local and remote independently deleted the same file or row | No mutation is produced; the plan records `already-in-sync` and preserves the remote snapshot. | `recognizes matching independent deletions as already in sync` |
| Matching independent deletion while remote-only plugin files or metadata changed elsewhere | No mutation is produced for the deletion; the plan records `already-in-sync` for the deleted resource and still preserves the remote plugin changes. | `keeps remote-only plugin changes while recognizing a matching independent deletion` |
| Matching independent row deletion while remote-only plugin files or metadata changed elsewhere | No mutation is produced for the row deletion; the plan records `already-in-sync` for the deleted row and still preserves the remote plugin changes. | `keeps remote-only plugin changes while recognizing a matching independent row deletion` |
| Matching independent deletion and file type swap while remote-only plugin files or metadata changed elsewhere | No mutation is produced for either matching resource; the plan records `already-in-sync` for the deletion and type swap and still preserves the remote plugin changes. | `keeps remote-only plugin changes while recognizing matching independent deletions and type swaps` |
| Matching independent deletion, type swap, and edit while remote-only plugin files or metadata changed elsewhere | No mutation is produced for the matching deletion, type swap, or edit; only the descendant deletion remains a mutation with live remote preconditions, and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, type swap, and matching edit stay safe` |
| Matching independent deletion, matching edit, and file type swap while remote-only plugin files changed elsewhere | No mutation is produced for the matching deletion, edit, or type swap; the remote-only plugin file drift stays `keep-remote`; and the ready plan remains bounded by live remote preconditions. | `keeps remote-only plugin changes while a matching delete, edit, and type swap stay already in sync` |
| Live-preconditioned delete, matching independent edit, and file type swap while remote-only plugin files changed elsewhere | The delete is the only mutation with a live remote precondition; the matching edit and type swap stay `already-in-sync`; and the remote-only plugin file drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe` |
| Live-preconditioned file delete, matching independent edit, and matching type swap while remote-only plugin drift changed elsewhere | The file delete is the only mutation with a live remote precondition; the matching edit and type swap stay `already-in-sync`; and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Live-preconditioned file delete and file type swap while a matching independent edit and remote-only plugin drift are present | Both mutations stay behind live remote preconditions, the matching independent edit stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while live-preconditioned file delete and type swap mutations preserve a matching independent edit` |
| Live-preconditioned file delete, matching independent row edit, and matching file type swap while remote-only plugin drift changed elsewhere | The file delete is the only mutation with a live remote precondition; the matching row edit and type swap stay `already-in-sync`; and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row edit, and matching file type swap stay safe with apply verification` |
| Live-preconditioned file type swap, matching independent edit, and matching row delete while remote-only plugin drift changed elsewhere | The file type swap is the only mutation with a live remote precondition; the matching edit and row delete stay `already-in-sync`; and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file type swap preserves a matching independent edit and delete` |
| Matching independent deletion, file type swap, and edit while remote-only plugin changes elsewhere | No mutation is produced for the matching deletion, type swap, or edit; the remote-only plugin drift stays `keep-remote`; and the plan remains bounded by live remote preconditions. | `keeps remote-only plugin changes while matching a deletion, file type swap, and edit stay already in sync` |
| Matching independent delete, edit, and file type swap while remote-only plugin changes elsewhere | No mutation is produced for the matching delete, edit, or type swap; the remote-only plugin drift stays `keep-remote`; and the plan remains bounded by live remote preconditions. | `keeps remote-only plugin changes while matching independent delete, edit, and type swap stay already in sync` |
| Matching independent deletion, create, edit, and file type swap while remote-only plugin changes elsewhere | No mutation is produced for the matching deletion, create, edit, or type swap; the remote-only plugin drift stays `keep-remote`; and the plan remains bounded by live remote preconditions. | `keeps remote-only plugin changes while matching deletion, create, edit, and type swap stay already in sync` |
| Matching independent deletion and edit while a live-preconditioned file type swap and remote-only plugin drift are present | The matching delete and edit stay `already-in-sync`; the type swap is the only mutation and keeps its live remote precondition; and remote-only plugin drift remains `keep-remote`. | `keeps remote-only plugin changes while a matching independent deletion, edit, and live-preconditioned type swap stay safe` |
| Live-preconditioned delete plus matching independent deletion, edit, and file type swap while remote-only plugin drift changed elsewhere | Only the local delete remains a mutation and keeps a live remote precondition; the matching deletion, edit, and type swap stay `already-in-sync`; and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent deletion, edit, and file type swap stay safe` |
| Live-preconditioned row delete plus matching independent row deletion, edit, and file type swap while remote-only plugin drift changed elsewhere | Only the row delete remains a mutation and keeps a live remote precondition; the matching row deletion, edit, and type swap stay `already-in-sync`; and the remote-only plugin drift stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row deletion, edit, and type swap stay safe` |
| Live-preconditioned delete plus matching independent deletion, edit, and file type swap while remote-only plugin files or metadata changed elsewhere | Only the local delete remains a mutation with a live remote precondition; the matching deletion, edit, and type swap stay `already-in-sync`; and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching deletion, edit, and type swap stay safe` |
| Matching independent restore, delete, edit, and file type swap while remote-only plugin files or metadata changed elsewhere | The unrelated deletion remains the only mutation with a live remote precondition; the restore, edit, and type swap all stay `already-in-sync`; and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching restore, edit, and type swap stay safe` |
| Matching independent row restore while remote-only plugin files or metadata changed elsewhere and an unrelated deletion is present | No mutation is produced for the matching row restore; the unrelated deletion remains the only mutation with a live remote precondition; and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a matching independent row restore and an unrelated deletion stay safe` |
| Matching independent deletion, edit, and file type swap while remote-only plugin removals changed elsewhere | No mutation is produced for the matching edit or type swap; the deletion remains the only mutation with a live remote precondition, and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while matching local delete, edit, and type swap stay hash-preconditioned` |
| Matching independent deletion, edit, and type swap while remote-only plugin removals changed elsewhere and apply is verified | No mutation is produced for the matching edit or type swap; the deletion remains the only mutation with a live remote precondition, the remote plugin removals stay `keep-remote`, and apply preserves them without losing the delete precondition. | `keeps remote-only plugin removals while matching independent delete, edit, and type swap stay safe` |
| Matching independent row deletion, edit, and file type swap while remote-only plugin removals changed elsewhere | No mutation is produced for the matching edit or type swap; the row deletion remains the only mutation with a live remote precondition, and the remote plugin removals stay `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned row delete, matching edit, and type swap stay safe` |
| Local file delete would hide a remote-only descendant while remote-only plugin drift exists elsewhere | The planner stops on the delete with bounded file-topology conflict evidence naming the descendant path, preserves the remote-only descendant, and keeps unrelated plugin drift `keep-remote`. | `stops a local file delete that would hide a remote-only descendant while keeping plugin drift and bounded evidence` |
| Local file delete and remote type swap target the same path while unrelated plugin drift exists elsewhere | The delete stops as `file-conflict`, the unrelated matching edit stays `already-in-sync`, the remote plugin drift stays `keep-remote`, and no mutation is emitted. | `stops a local file delete when the remote independently type swaps the same path while unrelated plugin drift is preserved` |
| Local file delete and remote type swap target the same path while a matching edit and remote-only plugin drift exist elsewhere | The delete stops as `file-conflict`, the matching edit stays `already-in-sync`, the remote plugin drift stays `keep-remote`, and the plan keeps bounded evidence without exposing payloads. | `stops a local file delete when the remote turned the same file into a directory while preserving a matching independent edit and remote-only plugin drift` |
| Local file delete and remote type swap target the same path while a matching independent delete and remote-only plugin drift exist elsewhere | The delete stops as `file-conflict`, the matching independent delete stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and the plan keeps bounded evidence without exposing payloads. | `stops a local file delete when the remote turned the same file into a directory while preserving a matching independent delete and remote-only plugin drift` |
| Local file delete and remote type swap target the same path while a matching independent edit and remote-only plugin drift exist elsewhere | The delete stops as `file-conflict`, the matching independent edit stays `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and the plan keeps bounded evidence without exposing payloads. | `stops a local file delete when the remote independently type swaps the same path while preserving a matching independent edit and remote-only plugin drift` |
| Local file delete and remote type swap target the same path while a matching independent edit, matching independent row delete, and remote-only plugin drift exist elsewhere | The delete stops as `file-conflict`, the matching edit and row delete stay `already-in-sync`, the remote-only plugin drift stays `keep-remote`, and the plan keeps bounded evidence without exposing payloads. | `stops a local file delete when the remote independently type swaps the same path while preserving matching independent edit, matching independent row delete, and remote-only plugin drift with bounded evidence` |
| Local file delete conflict while a matching independent type swap and remote-only plugin drift exist elsewhere | The delete is refused with bounded conflict evidence, the matching type swap stays `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `stops a local file delete conflict while preserving a matching independent type swap and remote-only plugin drift` |
| Local file delete conflict while a matching independent edit, type swap, and remote-only plugin drift exist elsewhere | The delete is refused with bounded conflict evidence, the matching edit and type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `stops a local file delete conflict while preserving a matching independent edit, type swap, and remote-only plugin drift` |
| Local file type swap would hide a remote-only descendant while a matching edit and remote-only plugin drift exist elsewhere | The planner stops on the file type swap with bounded file-topology conflict evidence naming the descendant path, preserves the matching edit as `already-in-sync`, and keeps remote-only plugin drift `keep-remote`. | `stops a file type swap that would hide a live remote descendant while preserving a matching edit and remote-only plugin drift` |
| Local and remote independently created the same file or row | No mutation is produced; the plan records `already-in-sync` and preserves the remote snapshot. | `recognizes matching independent file creations as already in sync` |
| Local deleted a directory, remote added a descendant under it | Plan is `conflict`; no mutation is emitted for the unsafe directory deletion; the remote-only descendant is preserved; conflict evidence names both paths without file contents. | `stops a local directory deletion that would remove a remote-only descendant` |
| Local file type swap would hide a remote-only descendant | Plan is `conflict`; no mutation is emitted for the unsafe type swap; remote descendant is preserved; conflict evidence names both paths without file contents. | `stops file type swaps that would hide remote-only descendants` |
| Local file type swap and descendant deletion both stay on the unchanged base while remote-only plugin files changed elsewhere | The file type swap is allowed because the descendant is deleted in the same plan from the unchanged base, both mutations keep live remote preconditions, and the remote-only plugin files stay `keep-remote`. | `allows a file type swap when the descendant is deleted in the same plan from the unchanged base` |
| Local directory deletion and matching descendant deletion while remote-only plugin files changed elsewhere | Both deletions are allowed because the descendant is deleted in the same plan from the unchanged base, both mutations keep live remote preconditions, and the remote-only plugin files stay `keep-remote`. | `keeps remote-only plugin changes while a local directory delete and matching descendant delete stay safe` |
| Local file delete, matching edit, and matching type swap while remote-only plugin removals changed elsewhere | The file delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and matching type swap stay safe with apply verification` |
| Local and remote independently performed the same file type swap | No mutation is produced; the plan records `already-in-sync` and preserves the remote snapshot. | `recognizes matching independent file type swaps as already in sync` |
| Local file type swap against an unchanged live remote file | The swap is planned as a mutation only when the remote file still matches base and the live precondition is attached to the file resource. | `plans local file type swaps only behind live remote preconditions` |
| Local ordinary file edit plus unsafe directory deletion | Plan is `conflict`; the unsafe topology mutation is suppressed, the independent mutation remains hash-preconditioned for audit, and apply refuses the whole non-ready plan. | `keeps independent mutation evidence while suppressing unsafe topology mutations` |
| Local ordinary file edit plus unsafe directory deletion while remote-only plugin metadata or file changed elsewhere | Plan is `conflict`; the unsafe topology mutation is suppressed, the independent mutation remains hash-preconditioned for audit, the remote plugin changes stay `keep-remote`, and apply refuses the whole non-ready plan. | `keeps remote-only plugin changes while suppressing unsafe topology mutations` |
| Remote and local independently changed a resource to identical content | No mutation is produced; plan records `already-in-sync`. | `recognizes matching independent edits as already in sync` |
| Remote and local independently changed plugin context to identical content | No mutation is produced; plan records `already-in-sync` for both the plugin metadata and the plugin file. | `recognizes matching independent plugin context edits as already in sync` |
| Remote and local independently deleted the same row | No mutation is produced; plan records `already-in-sync` with delete-vs-delete evidence. | `recognizes matching independent row deletions as already in sync` |
| Remote and local independently deleted the same file while remote-only plugin drift exists elsewhere | No mutation is produced; the file delete records `already-in-sync` with delete-vs-delete evidence, and the remote-only plugin drift stays `keep-remote`. | `recognizes matching independent file deletions as already in sync` |
| Remote and local independently restored the same file while remote-only plugin drift exists elsewhere | No mutation is produced; the file restore records `already-in-sync` with update-vs-update evidence, and the remote-only plugin drift stays `keep-remote`. | `recognizes matching independent file restores as already in sync while remote-only plugin drift is preserved` |
| Local postmeta references a post whose live remote target was deleted | Plan is `blocked`; the stale graph reference is refused, reference evidence names the deleted target without post contents, and apply refuses. | `blocks local featured-image references when the remote deleted the referenced post` |
| Remote-only plugin metadata or file changed | Remote plugin state is kept; no local mutation is produced. | `preserves remote-only plugin changes` |
| Remote-only plugin removal | Remote plugin deletion is kept; no local mutation is produced. | `preserves remote-only plugin removals` |
| Remote-only plugin removal while local edits an ordinary resource | The ordinary local mutation can be planned with a live remote precondition while remote plugin removal stays preserved as `keep-remote` decisions. | `preserves remote-only plugin removals while still applying independent local changes` |
| Remote-only plugin removal while local edits plugin-owned data | Plan is `blocked`; the stale plugin-owned mutation is refused, bounded owner-context evidence names the removed plugin and its file without row payloads, and apply refuses. | `blocks plugin-owned data when the live remote removed the owner plugin` |
| Remote-only plugin removal while a blocked plugin-owned mutation coexists with matching independent delete, edit, and file type swap | Plan is `blocked`; the unsupported plugin-owned mutation is refused, the matching delete, edit, and type swap stay `already-in-sync`, and the remote-only plugin removal stays `keep-remote`. | `blocks unsupported plugin-owned option updates while preserving matching independent delete, edit, type swap, and remote-only plugin removal` |
| Allowed plugin-owned delete while matching independent edit, file type swap, and remote-only plugin drift exist elsewhere | The delete is the only mutation, it stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `applies an allowed plugin-owned option delete only behind a live precondition while preserving matching independent edits and remote-only plugin drift` |
| Remote-only plugin removal while many plugin-context resources are present | Plan is `blocked`; owner-context evidence is truncated to a bounded slice with a truncation marker and no raw plugin payload leakage. | `bounds stale plugin owner evidence when many owner-context resources are present` |
| Remote-only plugin removal while a live-preconditioned delete and matching independent edit stay safe | The delete is the only mutation and stays behind a live remote precondition, the matching edit is `already-in-sync`, and the remote plugin removal stays `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned delete and matching independent edit stay safe` |
| Remote-only plugin change while a live-preconditioned delete, matching row deletion, edit, and file type swap stay safe | The delete is the only mutation and stays behind a live remote precondition, the matching row deletion, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent row deletion, edit, and type swap stay safe with apply verification` |
| Remote-only plugin removal while a live-preconditioned row delete, matching edit, and type swap stay safe | The row delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin removal stays `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned row delete, matching edit, and type swap stay safe with apply verification` |
| Remote-only plugin change while a live-preconditioned delete and matching deletion and type swap stay safe | The delete is the only mutation and stays behind a live remote precondition, the matching deletion and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete and matching deletion and type swap stay safe` |
| Remote-only plugin change while a live-preconditioned delete, matching restore, and type swap stay safe | The delete is the only mutation and stays behind a live remote precondition, the matching restore and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching restore, and type swap stay safe` |
| Remote-only plugin change while a live-preconditioned delete and matching independent edit stay safe | The delete is the only mutation and stays behind a live remote precondition, the matching edit stays `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent edit stay safe` |
| Remote-only plugin changes while a live-preconditioned file delete and matching independent delete, restore, edit, and type swap stay safe | The file delete is the only mutation and stays behind a live remote precondition, the matching delete, restore, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent delete, restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin metadata while a live-preconditioned delete and matching independent edit and type swap stay safe | The delete is the only mutation and stays behind a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin metadata stays `keep-remote`. | `keeps remote-only plugin metadata while a live-preconditioned delete and matching independent edit and type swap stay safe` |
| Remote-only plugin removal while local edits the removed plugin's file | Plan is `conflict`; the stale local plugin file mutation is not emitted; conflict evidence names the plugin owner context and hashes without file contents. | `stops stale local plugin file edits when the remote removed that plugin` |
| Remote-only plugin removal while local and remote independently delete, edit, and type-swap ordinary resources | The matching ordinary resources stay `already-in-sync`, the remote plugin removal stays `keep-remote`, no mutation is produced, and apply preserves the remote removal snapshot. | `keeps remote-only plugin removals while matching independent delete, edit, and type swap stay already in sync` |
| Remote-only plugin removal while local and remote independently delete, edit, and type-swap ordinary resources plus a stale plugin mutation is absent | The matching ordinary resources stay `already-in-sync`, the remote plugin removal stays `keep-remote`, and the planner emits no stale plugin mutation candidate. | `preserves remote-only plugin removals while matching independent delete, edit, and type swap stay already in sync` |
| Remote-only plugin change while a live-preconditioned file delete, matching independent row restore, and matching file type swap stay safe | The file delete is the only mutation and keeps a live remote precondition, the matching row restore and file type swap stay `already-in-sync`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row restore, and matching file type swap stay safe with apply verification` |
| File delete conflict while remote-only plugin removals and a matching independent edit are present | The file delete stops with bounded file-conflict evidence, the matching edit stays `already-in-sync`, and the remote-only plugin removal stays preserved as `keep-remote`. | `stops a file delete conflict while preserving remote-only plugin removals and matching independent edits` |
| File delete conflict while matching independent edit and type swap stay safe and remote-only plugin removals are present | The file delete stops with bounded file-conflict evidence, the matching edit and type swap stay `already-in-sync`, and the remote-only plugin removals stay preserved as `keep-remote`. | `stops a local file delete conflict while preserving matching independent edit and type swap plus remote-only plugin removals` |
| Remote-only plugin removal while mixed ordinary local mutations remain safe | The local deletion is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin removal stays `keep-remote`, and the mutation keeps a live remote precondition. | `keeps remote-only plugin removals while mixed ordinary local mutations stay hash-preconditioned` |
| Remote-only plugin removal while a live-preconditioned delete, matching independent delete, edit, and type swap stay safe | The live-preconditioned delete is the only mutation, the matching independent deletion, edit, and type swap stay `already-in-sync`, the remote plugin removal stays `keep-remote`, and the delete keeps a live remote precondition. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent delete, edit, and type swap stay safe` |
| Remote-only plugin metadata change while mixed ordinary local mutations remain safe | The local deletion is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin metadata change stays `keep-remote`, and the mutation keeps a live remote precondition. | `keeps remote-only plugin metadata changes while mixed ordinary local mutations stay hash-preconditioned` |
| Remote-only plugin metadata or file changed while local delete, matching edit, and matching type swap remain safe | The local delete is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while local delete, matching edit, and matching type swap stay live-preconditioned` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete and matching independent edit and type swap remain safe | The delete is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent edit and type swap stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete and matching independent edit and type swap remain safe, with apply verification | The delete is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and apply preserves the remote-only plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete, matching independent file deletion, edit, and type swap remain safe | The delete is the only mutation, the matching file deletion, edit, and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent file deletion, edit, and type swap stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete, matching independent restore, and matching independent type swap remain safe | The delete is the only mutation, the matching restore and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and matching independent type swap stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete, matching independent row deletion, edit, and type swap stay safe with apply verification | The file delete is the only mutation, the matching row deletion, edit, and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the file delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row deletion, edit, and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned type swap and matching independent edit remain safe | The type swap is the only mutation, the matching edit stays `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the type swap keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned type swap and matching independent edit stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned row delete and matching independent edit and type swap remain safe | The row deletion is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the row delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned row delete and matching independent edit and type swap stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned row delete and matching file edit and file type swap remain safe | The row deletion is the only mutation, the matching file edit and file type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the row delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned row delete and matching file edit and type swap stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete and matching independent edit and type swap remain safe | The file deletion is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin metadata or file change stays `keep-remote`, and the file delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent edit and type swap stay safe` |
| Remote-only plugin removals while a live-preconditioned file delete and matching independent delete, edit, and type swap stay safe with apply verification | The file delete is the only mutation and stays behind a live remote precondition, the matching delete, edit, and type swap stay `already-in-sync`, the remote plugin removals stay `keep-remote`, and apply preserves the removed plugin snapshot. | `keeps remote-only plugin removals while a live-preconditioned file delete and matching independent delete, edit, and type swap stay safe with apply verification` |
| Remote-only plugin removals while a live-preconditioned delete, matching independent restore, and type swap stay safe | The delete and type swap are the only mutations, each keeps a live remote precondition, the matching restore stays `already-in-sync`, and apply preserves the removed plugin state. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent restore, and type swap stay safe` |
| Remote-only plugin removals while a live-preconditioned delete, matching independent restore, and matching type swap stay safe with apply verification | The delete is the only mutation and keeps a live remote precondition, the matching restore and type swap stay `already-in-sync`, and apply preserves the removed plugin state. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent restore, and matching type swap stay safe with apply verification` |
| Remote-only plugin removal while an unrelated delete, matching edit, and matching type swap remain safe | The unrelated deletion is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin removal stays `keep-remote`, and the delete keeps a live remote precondition. | `keeps remote-only plugin removals while an unrelated delete, matching edit, and matching type swap stay safe` |
| Remote-only plugin removal while an unrelated delete, matching edit, and matching type swap remain safe with apply verification | The unrelated deletion is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin removal stays `keep-remote`, and apply preserves the remote plugin removal without losing the live remote precondition. | `keeps remote-only plugin removals while a mixed delete, edit, and type swap remains live-preconditioned` |
| Remote-only plugin removal while a live-preconditioned delete, matching edit, and type swap stay safe | The delete is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin removal stays `keep-remote`, and apply preserves the remote removal snapshot. | `preserves remote-only plugin removals while a live-preconditioned delete, matching edit, and type swap stay safe` |
| Remote-only plugin change while a live-preconditioned delete, matching independent edit, and file type swap stay safe | The delete and type swap are the only mutations, both keep live remote preconditions, the matching edit stays `already-in-sync`, and the remote plugin change stays `keep-remote` through apply verification. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Remote-only plugin change while a matching delete, edit, and type swap remain safe | The matching delete, edit, and type swap stay `already-in-sync`, the remote plugin change stays `keep-remote`, and no mutation is produced. | `keeps remote-only plugin changes while matching independent delete, edit, and type swap stay already in sync` |
| Remote-only plugin change while a live-preconditioned delete, matching edit, and file type swap stay safe | The delete and type swap are the only mutations, both keep live remote preconditions, the matching edit stays `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching edit, and file type swap stay safe` |
| Remote-only plugin change while a live-preconditioned delete, matching independent delete, edit, and file type swap stay safe | The file delete is the only mutation and keeps a live remote precondition, the matching independent delete, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent delete, edit, and type swap stay safe` |
| Remote-only plugin change while a live-preconditioned delete, matching independent file delete, edit, and file type swap stay safe | The file delete is the only mutation and keeps a live remote precondition, the matching independent file delete, edit, and file type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent delete, edit, and file type swap stay safe` |
| Remote-only plugin change while a live-preconditioned file delete and matching independent row delete, edit, and type swap stay safe | The file delete is the only mutation and keeps a live remote precondition, the matching independent row delete, edit, and type swap stay `already-in-sync`, and the remote plugin change stays `keep-remote`. | `keeps remote-only plugin changes while matching independent delete, edit, and file type swap stay already in sync beside a live-preconditioned file delete` |
| Remote-only plugin change while a matching deletion, restore, and type swap remain safe | The matching delete, restore, and type swap stay `already-in-sync`, the remote plugin change stays `keep-remote`, and no mutation is produced. | `keeps remote-only plugin changes while matching deletion, restore, and type swap stay already in sync` |
| Remote-only plugin change while a matching independent file creation stays safe | The matching file creation stays `already-in-sync`, the remote plugin change stays `keep-remote`, and no mutation is produced. | `keeps remote-only plugin changes while recognizing a matching independent file creation` |
| Remote-only plugin change while a matching deletion and restore stay safe | The matching delete and restore stay `already-in-sync`, the remote plugin change stays `keep-remote`, and the only mutation keeps a live remote precondition. | `keeps remote-only plugin changes while recognizing a matching independent deletion and restore` |
| Remote-only plugin change while a matching row deletion, edit, and type swap remain safe | The matching row deletion, edit, and type swap stay `already-in-sync`, the remote plugin change stays `keep-remote`, and no mutation is produced. | `keeps remote-only plugin changes while matching independent row delete, edit, and type swap stay already in sync` |
| Remote-only plugin metadata or file changed while local deletes an ordinary file | The local ordinary deletion can proceed with a live remote precondition while remote-only plugin changes are kept as `keep-remote` decisions. | `preserves remote-only plugin changes while local deletes an ordinary file` |
| Remote-only plugin metadata or file changed while local deletes an ordinary row | The local row deletion can proceed with a live remote precondition while remote-only plugin changes are kept as `keep-remote` decisions. | `keeps remote-only plugin changes while planning an unrelated local row deletion` |
| Remote-only plugin metadata or file changed while local edits an ordinary resource | The ordinary local mutation can be planned with a live remote precondition while remote plugin metadata/files are kept as `keep-remote` decisions. | `combines local ordinary changes while preserving remote-only plugin changes` |
| Remote-only plugin metadata or file changed while a stale plugin-context edit is present alongside an unrelated ordinary mutation | The unrelated ordinary mutation stays as the only live-preconditioned mutation, and the stale plugin-context edit becomes a conflict without leaking raw plugin payloads. | `preserves unrelated ordinary mutations while stopping stale plugin-context edits` |
| Remote-only plugin metadata or file changed while local and remote independently reach the same ordinary edit | No mutation is produced for the matching ordinary edit; the plan records `already-in-sync` for that resource and still preserves the remote plugin changes. | `keeps remote-only plugin changes while recognizing a matching independent edit` |
| Remote-only plugin metadata or file changed while local deletion, matching edit, and matching type swap remain safe | The deletion is the only mutation and keeps a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned delete, matching edit, and matching type swap stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification | The delete is the only mutation and keeps a live remote precondition, the matching edit and type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent edit, and file type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned row delete, matching independent row deletion, edit, and type swap stay safe with apply verification | The row delete is the only mutation and keeps a live remote precondition, the matching row deletion, edit, and type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row deletion, edit, and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned row delete and matching independent edit and type swap stay safe with apply verification | The row delete is the only mutation and keeps a live remote precondition, the matching edit and type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned row delete and matching independent edit and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete, matching independent row deletion, matching independent restore, and type swap stay safe with apply verification | The file delete is the only mutation and keeps a live remote precondition, the matching row deletion, restore, and type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching independent row deletion, matching independent restore, and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned row delete and matching independent edit and type swap remain safe | The row deletion is the only mutation and keeps a live remote precondition, the matching edit and type swap stay `already-in-sync`, and the remote plugin changes stay `keep-remote`. | `keeps remote-only plugin changes while a live-preconditioned row delete and matching independent edit and type swap stay safe` |
| Plugin-owned option deletion without delete opt-in | The planner blocks the stale delete instead of assuming plugin-owned rows may be removed safely. | `blocks plugin-owned option deletions without explicit delete opt-in` |
| Plugin-owned delete with explicit delete opt-in after remote plugin removal | The planner still stops on the stale owner context, preserves the unrelated safe edit as evidence, and refuses to guess that the remote plugin removal makes the delete safe. | `blocks plugin-owned deletes when the owner plugin was removed remotely even with an unrelated safe edit` |
| Plugin-owned delete with explicit delete opt-in after remote plugin removal while a matching independent edit stays safe | The planner still stops on the stale owner context, preserves the matching independent edit as `already-in-sync`, and refuses to guess that the remote plugin removal makes the delete safe. | `blocks plugin-owned deletes when the owner plugin was removed remotely while preserving a matching independent edit` |
| Plugin-owned delete with explicit delete opt-in after remote plugin removal while matching independent restore, edit, and type swap stay safe | The planner still stops on the stale owner context, preserves the matching independent restore, edit, and type swap as `already-in-sync`, and refuses to guess that the remote plugin removal makes the delete safe. | `blocks plugin-owned deletes when the owner plugin was removed remotely while preserving matching independent restore, edit, and type swap` |
| Plugin-owned delete with matching independent edit, file type swap, and remote-only plugin drift | The plugin-owned delete stays behind a live remote precondition, the matching independent edit and file type swap stay `already-in-sync`, and the remote-only plugin drift stays `keep-remote`. | `applies plugin-owned deletes when a matching independent edit, file type swap, and remote-only plugin drift are present` |
| Plugin-owned delete with matching independent delete, restore, and file type swap | Plan is `blocked`; the matching independent delete, restore, and type swap stay `already-in-sync`, remote-only plugin drift stays `keep-remote`, and the stale owner-context evidence stays bounded. | `blocks plugin-owned deletes when matching independent delete, restore, and file type swap are present` |
| Stale plugin-owned delete while a matching independent edit stays safe and remote-only plugin drift exists elsewhere | The planner must stop on the stale owner context, keep the matching independent edit `already-in-sync`, and preserve the remote-only plugin drift without emitting the owned delete. | `blocks a stale plugin-owned delete while preserving a matching edit and remote-only plugin drift` |
| Remote-only plugin metadata or file changed while local and remote independently reach the same file type swap | No mutation is produced for the matching file type swap; the plan records `already-in-sync` for that resource and still preserves the remote plugin changes. | `keeps remote-only plugin changes while recognizing a matching independent file type swap` |
| Remote-only plugin metadata or file changed while local and remote independently recreate the same file | No mutation is produced for the restore-style create; the plan records `already-in-sync` for that resource and still preserves the remote plugin changes. | `keeps remote-only plugin changes while recognizing a matching independent restore` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete, matching row restore, and type swap stay safe with apply verification | The file delete is the only mutation and keeps a live remote precondition, the matching row restore and type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete, matching row restore, and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned row delete, matching independent row delete, restore, edit, and file type swap stay safe with apply verification | The row delete is the only mutation and keeps a live remote precondition, the matching row delete, restore, edit, and file type swap stay `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned row delete, matching independent row delete, restore, edit, and type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete and matching independent row delete stay safe with apply verification | The file delete is the only mutation and keeps a live remote precondition, the matching row delete stays `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent row delete stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete and matching restore stay safe with apply verification | The file delete is the only mutation and keeps a live remote precondition, the matching restore stays `already-in-sync`, and apply preserves the remote plugin drift. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching restore stay safe with apply verification` |
| Remote-only plugin removals while a live-preconditioned file delete and matching restore stay safe with apply verification | The file delete is the only mutation and keeps a live remote precondition, the matching restore stays `already-in-sync`, and apply preserves the remote plugin removals. | `keeps remote-only plugin removals while a live-preconditioned file delete and matching restore stay safe with apply verification` |
| Remote-only plugin metadata or file changed while local deletes an unrelated row and independently recreates the same file | The row deletion is the only mutation, the restore-style file create stays `already-in-sync`, the remote plugin changes stay `keep-remote`, and the delete keeps a live remote precondition. | `keeps remote-only plugin changes while mixing an unrelated deletion with a matching independent restore` |
| Remote-only plugin removals while local deletes a file and independently matches a file type swap and file edit | The delete is the only mutation and keeps a live remote precondition, the matching file type swap and file edit stay `already-in-sync`, and the remote plugin removal stays `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and type swap stay safe` |
| Remote-only plugin removals while local deletes a row and independently matches a file edit and type swap | The row deletion is the only mutation and keeps a live remote precondition, the matching file edit and type swap stay `already-in-sync`, and the remote plugin removal stays `keep-remote`. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and type swap stay safe` |
| Remote-only plugin metadata or file changed while local mixes a matching independent restore with an unrelated deletion | The deletion is the only mutation, the restore-style create stays `already-in-sync`, the remote plugin changes stay `keep-remote`, and the delete keeps a live remote precondition. | `keeps remote-only plugin changes while a matching independent restore and an unrelated deletion stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete and matching independent restore stay safe | The row deletion is the only mutation, the restore-style file create stays `already-in-sync`, the remote plugin changes stay `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent restore stay safe` |
| Remote-only plugin metadata or file changed while a live-preconditioned delete and matching independent edit stay safe with apply verification | The row deletion is the only mutation, the matching edit stays `already-in-sync`, the remote plugin changes stay `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned delete and matching independent edit stay safe with apply verification` |
| Remote-only plugin removals while a live-preconditioned delete and matching independent edit stay safe | The file delete is the only mutation, the matching edit stays `already-in-sync`, the remote plugin removals stay `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin removals while a live-preconditioned delete and matching independent edit stay safe` |
| Remote-only plugin removals while live-preconditioned file delete and file type swap mutations preserve a matching independent edit | Both file mutations stay behind live remote preconditions, the matching edit stays `already-in-sync`, and the remote plugin removals stay `keep-remote` through apply verification. | `keeps remote-only plugin removals while live-preconditioned file delete and file type swap mutations preserve a matching independent edit` |
| Remote-only plugin removals while a live-preconditioned delete and matching independent restore stay safe | The row deletion is the only mutation, the restore-style file create stays `already-in-sync`, the remote plugin removals stay `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin removals while a live-preconditioned delete and matching independent restore stay safe` |
| Remote-only plugin removals while a live-preconditioned delete, matching restore, and type swap stay safe | The row deletion is the only mutation, the restore-style file create and type swap stay `already-in-sync`, the remote plugin removals stay `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching restore, and type swap stay safe` |
| Remote-only plugin removals while a live-preconditioned delete, matching edit, and matching type swap stay safe with apply verification | The file delete is the only mutation, the matching edit and type swap stay `already-in-sync`, the remote plugin removals stay `keep-remote`, and apply preserves the removed plugin state. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and matching type swap stay safe with apply verification` |
| Remote-only plugin metadata or file changed while local mixes a deletion, matching independent edit, and matching independent file type swap | The deletion is the only mutation; the matching edit and type swap are `already-in-sync`, the remote plugin changes stay `keep-remote`, and the mutation keeps a live remote precondition. | `keeps remote-only plugin changes while matching local delete and type swap stay hash-preconditioned` |
| Remote-only plugin removals while a live-preconditioned delete, matching edit, and matching type swap stay safe | The file deletion is the only mutation; the matching edit and type swap stay `already-in-sync`; the remote plugin removals stay `keep-remote`; and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching edit, and matching type swap stay safe` |
| Remote-only plugin removals while a live-preconditioned delete, matching independent delete, and matching independent file type swap stay safe | The delete is the only mutation; the matching delete and file type swap stay `already-in-sync`; the remote plugin removals stay `keep-remote`; and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent delete, and matching independent file type swap stay safe` |
| Remote-only plugin removals while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification | The file deletion is the only mutation; the matching independent edit and type swap stay `already-in-sync`; the remote plugin removals stay `keep-remote`; and apply preserves the removed plugin state. | `keeps remote-only plugin removals while a live-preconditioned delete, matching independent edit, and matching type swap stay safe with apply verification` |
| Remote-only plugin drift while a live-preconditioned delete, matching restore, and matching independent file type swap stay safe | The file delete is the only mutation, the matching restore and type swap stay `already-in-sync`, the remote plugin drift stays `keep-remote`, and the emitted mutation keeps a live remote precondition. | `keeps remote-only plugin changes while a live-preconditioned delete, matching independent restore, and matching independent file type swap stay safe` |
| Remote-only plugin metadata or file changed while local deletes an unrelated resource | The unrelated deletion can still be planned with a live remote precondition while remote plugin metadata/files remain `keep-remote`. | `keeps remote-only plugin changes while allowing an unrelated local deletion` |
| Remote-only plugin metadata or file changed while local performs an unrelated file type swap | The file type swap can be planned with a live remote precondition while remote plugin metadata/files remain `keep-remote`. | `keeps remote-only plugin changes while allowing an unrelated local file type swap` |
| Remote-only plugin metadata or file changed while a live-preconditioned file delete and matching independent edit stay safe | The delete is the only mutation, the matching edit stays `already-in-sync`, the remote plugin changes stay `keep-remote`, and the delete keeps a live remote precondition through apply. | `keeps remote-only plugin changes while a live-preconditioned file delete and matching independent edit stay safe with apply verification` |
| Remote-only plugin metadata or file changed while a direct file conflict and an unrelated delete are both present | The unrelated delete remains the only mutation with a live remote precondition, the direct file conflict stops the plan, and the remote plugin drift stays `keep-remote` in the blocked evidence. | `blocks a direct file conflict while preserving an unrelated live delete and remote-only plugin drift` |
| Local plugin metadata changed while remote plugin files changed | Plan is `blocked`; the local metadata mutation is not emitted; evidence names the stale remote plugin file context without file contents. | `blocks local plugin metadata changes when remote plugin files changed` |
| Local plugin files changed while remote plugin metadata changed | Plan is `blocked`; the local file mutation is not emitted; evidence names the stale remote plugin metadata context without plugin payload values. | `blocks local plugin file changes when remote plugin metadata changed` |
| Remote-only plugin metadata or file changed while a stale plugin file edit and an unrelated deletion are both present | The unrelated deletion stays as the only mutation with a live remote precondition, the stale plugin file edit becomes a conflict, and apply refuses the non-ready plan. | `blocks stale plugin file changes while preserving an unrelated safe deletion as evidence` |
| Local plugin files changed while local and remote independently reached the same plugin metadata | File mutation may proceed with live remote preconditions because the plugin metadata context matches the live remote context. | `allows plugin file changes when plugin metadata independently matches remote` |
| Local plugin-owned data changed while the owner plugin file changed only on remote | Plan is `blocked`; the row mutation is not emitted; evidence names the owner plugin context resource and hashes without file contents. | `blocks plugin-owned data when owner plugin files changed only on remote` |
| Local plugin-owned data changed while local and remote independently reached the same owner plugin file | Row mutation may proceed with live remote preconditions because the local owner plugin context matches the live remote context. | `allows plugin-owned data when owner plugin context independently matches remote` |
| Remote-only plugin removal invalidates a stale local dependency | Plan is `blocked`; stale local plugin state cannot satisfy the dependency. | `remote-only plugin removal blocks stale local dependency assumptions` |
| Remote and local changed the same row differently | Plan is `conflict`; apply refuses; remote remains unchanged. | `refuses direct conflicts and preserves the remote snapshot` |
| Conflict is in plugin-owned data | Conflict is classified as `plugin-data-conflict`. | `classifies plugin-owned data conflicts separately from generic rows` |
| Plugin-owned forms option is allowlisted fixture data | Nested `reprint_push_forms_fixture` data can be planned and applied only through the explicit fixture allowlist. | `npm run test:playground` / Playground plugin-owned forms fixture smokes |
| Plugin-owned forms postmeta is allowlisted fixture data | `_reprint_push_forms_schema` postmeta can be applied only when attached to fixture-marked parent posts. | `npm run test:playground` / Playground plugin-owned forms fixture smokes |
| Plugin-owned row declares the wrong driver for its table | Plan is `blocked` before mutation; evidence includes the resource key, declared driver, hashes, and change kind without row payload values. | `blocks plugin-owned resources when the declared driver does not match the table` |
| Fixture forms lab custom-table driver | Exact `wp_reprint_push_forms_lab` positive `id:N` rows can become ready only with owner `forms`, explicit `fixture-forms-lab-table` policy, unchanged active `reprint-push-forms-fixture` evidence, and matching precondition hashes. Delete remains blocked. | `fixture forms lab table requires exact driver and active fixture plugin evidence` / `npm run test:playground:forms-lab-table` |
| Unknown plugin-owned custom-table rows block | Custom tables outside the exact forms lab fixture driver, missing driver policy, forged driver evidence, or forged generic drivers such as `wp-option` on the custom table reject before mutation as unsupported plugin-owned resources. | `blocks unknown plugin-owned custom table rows without leaking values` / `npm run test:playground:forms-lab-table` |
| Fixture forms lab row conflict/stale safety | Divergent local/remote forms lab rows stay `plugin-data-conflict`; stale preconditions reject with `PRECONDITION_FAILED` and preserve the target. Journal/recovery evidence is hash-only/redacted for raw row payload values. | `fixture forms lab table conflicts and stale preconditions preserve remote` / `fixture forms lab table blocked recovery redacts raw remote payload values` |
| Plugin metadata is detection-only | `reprint-push-forms-fixture` metadata is exported/detected but not applied as a ready mutation. | `npm run test:playground` / Playground plugin-owned forms fixture smokes |
| Plugin-owned mutation conflicts after the owner plugin is removed remotely | The unrelated safe file edit stays `already-in-sync`, remote-only plugin drift stays `keep-remote`, and the planner stops on stale plugin-owner context instead of risking the owned row. | `blocks plugin-owned updates when the owner plugin was removed remotely while preserving unrelated safe edits and remote-only plugin drift` |
| Local WordPress graph row references a stale remote-created post identity | Plan is `blocked`; the local `wp_postmeta` mutation is not emitted; the remote-created `wp_posts` row is kept; blocker evidence records relationship key, target resource key, hashes, and change kinds without raw post or meta payload values. | `blocks local postmeta references to stale remote-created post identity` |
| Atomic plugin install is missing dependency | Plan is `blocked`; apply refuses. | `blocks an atomic plugin install when dependencies are absent` |
| Atomic plugin dependency metadata includes private fields | Plan evidence records only normalized plugin dependency audit fields and omits raw dependency payloads from blockers and atomic-group dependencies. | `redacts raw plugin dependency metadata from blocker evidence` |
| Plugin install and dependency are included together | All files, plugin metadata, and options apply as one atomic group. | `applies an atomic plugin install when dependencies are included in the same plan` |
| Plugin dependency mutation is outside the atomic group | Plan is `blocked`; apply refuses. | `blocks an atomic plugin bundle when its dependency mutation is outside the group` |
| Remote dependency changed since base | Plan is `blocked`; stale local dependency assumptions cannot make the bundle ready. | `blocks a dependent atomic bundle when a remote dependency changed since base` |
| Plugin dependency version range is incompatible | Plan is `blocked`; apply refuses. | `blocks an atomic bundle with an incompatible plugin dependency version range` |
| Plugin dependency hash metadata does not match remote | Plan is `blocked`; apply refuses. | `blocks an atomic bundle when dependency hash metadata does not match remote` |
| Remote changes after dry-run | Apply rejects with `PRECONDITION_FAILED`. | `rejects apply when the remote changed after dry-run planning` |
| Playground fixture protocol dry-run | Dry-run verifies ready-plan preconditions, applies nothing, and same-process WordPress readback stays unchanged. | `npm run test:playground` / `scripts/playground/push-protocol-smoke.mjs` |
| Playground fixture protocol ready apply | Apply with a supplied dry-run receipt writes the eight expected fixture mutations and verifies hashes/readback. | `npm run test:playground` / `scripts/playground/push-protocol-smoke.mjs` |
| Playground fixture protocol missing receipt | Apply without a supplied dry-run receipt rejects with `MISSING_DRY_RUN_RECEIPT` before mutation. | `npm run test:playground` / `scripts/playground/push-protocol-smoke.mjs` |
| Playground fixture protocol tampered receipt | Apply with a mismatched receipt rejects with `RECEIPT_MISMATCH` before mutation. | `npm run test:playground` / `scripts/playground/push-protocol-smoke.mjs` |
| Playground fixture protocol stale apply | Stale apply rejects with `PRECONDITION_FAILED` and preserves the changed remote fixture. | `npm run test:playground` / `scripts/playground/push-protocol-smoke.mjs` |
| Playground fixture protocol non-ready plan | Conflict dry-run and apply reject with `PLAN_NOT_READY` and report row, file, and plugin-data conflict classes. | `npm run test:playground` / `scripts/playground/push-protocol-smoke.mjs` |
| Playground local REST namespace | Disposable Playground servers expose `reprint-push-lab/v1` with `GET /snapshot`, `GET /journal`, `POST /dry-run`, and `POST /apply` over real local HTTP. | `npm run test:playground:http-push` |
| Playground local REST ready apply | Dry-run is read-only, returns a receipt, and receipt-backed apply writes the eight expected fixture mutations. | `npm run test:playground:http-push` |
| Playground local REST receipt/stale/conflict refusals | Missing receipt rejects with `428 MISSING_DRY_RUN_RECEIPT`, tampered receipt with `409 RECEIPT_MISMATCH`, stale remote with `412 PRECONDITION_FAILED`, and conflict plans with `409 PLAN_NOT_READY` for row, file, and plugin-data classes. | `npm run test:playground:http-push` |
| Playground authenticated REST namespace | Authenticated local routes live under `/wp-json/reprint-push-lab/v1/authenticated/*`; public legacy lab routes remain intentionally public for older smokes. | `npm run test:playground:authenticated-http-push` |
| Playground authenticated preflight | Basic-auth-shaped WordPress Application Password credentials for bootstrapped users establish identity, require `manage_options`, and return identity/capability/scope/session/expiry/journal evidence. Playground fallback caveat: local Playground core did not establish `/wp-json/wp/v2/users/me`, so the lab route validates stored hashed app-password entries and sets the current user before capability checks. | `npm run test:playground:authenticated-http-push` |
| Playground signed request integrity | `/authenticated/preflight`, `/authenticated/dry-run`, and `/authenticated/apply` require signed lab requests. Signature verification happens before JSON parsing, receipt validation, idempotency lookup/claim, journal writes, or mutation. `X-Auth-Content-Hash` is SHA-256 over raw request body bytes. | `npm run test:playground:authenticated-http-push` |
| Playground push signature binding | Auth signatures cover nonce, timestamp, and content hash. Push signatures bind method, actual path, canonical query, content hash, server-minted lab session, and idempotency key; dry-run/apply require the session and idempotency key. | `npm run test:playground:authenticated-http-push` |
| Playground authenticated dry-run receipt | Authenticated dry-run is read-only by authenticated snapshot comparison and mints a receipt bound to auth scope, identity, session, request route, plan, and body. | `npm run test:playground:authenticated-http-push` |
| Playground authenticated apply | Authenticated apply requires `X-Reprint-Push-Idempotency-Key`, validates receipt scope/expiry/identity/session/request binding before DB idempotency claim and mutation, applies over real local HTTP, and verifies source changes through a fresh authenticated snapshot. | `npm run test:playground:authenticated-http-push` |
| Playground authenticated negative auth cases | Missing, bad, and malformed auth reject; insufficient capability rejects or remains auth-required if the limited Playground identity cannot be established; forged `reprint_push_lab_auth` query/body/header values do not bypass auth. | `npm run test:playground:authenticated-http-push` |
| Playground authenticated signature negative cases | Unsigned, malformed, bad hash, body changed after signing, stale/future timestamp, wrong method/path/query, wrong session, idempotency mismatch, public-route signature attempts, and nonce replay reject before mutation. Nonce replay rejects before idempotency replay. | `npm run test:playground:authenticated-http-push` |
| Playground authenticated receipt/stale/replay cases | Tampered or wrong-identity receipts reject with `AUTH_RECEIPT_MISMATCH`, expired receipts reject with `AUTH_RECEIPT_EXPIRED`, missing idempotency key rejects before mutation, stale remote state preserves target data and creates no idempotency claim, and replay with a fresh nonce/signature performs zero fresh mutation work. | `npm run test:playground:authenticated-http-push` |
| Authenticated CLI dry-run | `reprint-push-lab push-authenticated --dry-run-only` fetches the live source snapshot, builds a three-way plan, signs preflight/dry-run, returns an auth-bound receipt hash, and leaves the source unchanged. | `npm run test:playground:authenticated-cli-push` |
| Authenticated CLI apply | `reprint-push-lab push-authenticated` performs the same authenticated plan/dry-run path, applies with an idempotency key, records DB journal commit evidence, and verifies the final source snapshot against the local fixture surface. | `npm run test:playground:authenticated-cli-push` |
| Authenticated CLI changed-source refusal | When the source changed since the pull base, the CLI reports `PLAN_NOT_READY_LOCALLY` with conflict evidence and does not call dry-run or apply. | `npm run test:playground:authenticated-cli-push` |
| Authenticated CLI post-snapshot drift refusal | When the source changes after the CLI fetched its live snapshot but before dry-run, the locally ready plan is refused by authenticated dry-run with `PRECONDITION_FAILED`; apply is not called and the concurrent source change is preserved. | `npm run test:playground:authenticated-cli-push` |
| Playground DB journal requires idempotency key | `POST /apply` without `X-Reprint-Push-Idempotency-Key` rejects with `400 MISSING_IDEMPOTENCY_KEY` before mutation. | `npm run test:playground:db-journal-idempotency` |
| Playground DB journal records apply boundaries | `wp_reprint_push_lab_push_journal` records `idempotency-opened`, `apply-started`, per-mutation `mutation-prepared`, per-mutation `mutation-applied`, `apply-committed`, replay, and conflict evidence separately from the legacy `wp_options` `/journal` evidence; compact mutation evidence stores hashes/metadata only. | `npm run test:playground:db-journal-idempotency` |
| Playground DB idempotency replay | Same key plus same body returns `BATCH_ALREADY_COMMITTED` with `idempotency.replayed: true`, no fresh mutation work, no extra mutation events, and an unchanged snapshot. | `npm run test:playground:db-journal-idempotency` |
| Playground DB idempotency conflict | Same key plus a different body rejects with `409 IDEMPOTENCY_KEY_CONFLICT` before mutation. | `npm run test:playground:db-journal-idempotency` |
| Playground DB stale rejection replay | A stale precondition failure is journaled as a rejected terminal result; same key/body replay returns `PRECONDITION_FAILED` with `idempotency.replayed: true` and no fresh mutation work, while a different body with the same key conflicts. | `npm run test:playground:db-journal-idempotency` |
| Playground mid-apply JIT drift | A lab drift after dry-run and initial apply validation but before mutation `N` writes returns `412 PRECONDITION_FAILED`, preserves the drifted target, records hash-only `mutation-precondition-failed` evidence, writes no `mutation-applied` for `N`, writes no later mutations, and writes no `apply-committed`. | `npm run test:playground:mid-apply-drift` |
| Playground mid-apply rejected replay | Same key/body after the mid-apply JIT rejection replays the rejected result with `idempotency.replayed: true` and no fresh mutation work; same key/different body returns `409 IDEMPOTENCY_KEY_CONFLICT`; recovery inspect is non-mutating. | `npm run test:playground:mid-apply-drift` |
| Playground storage-boundary guarded DB updates | After the JIT pre-write hash passes, existing fixture row updates for `wp_posts`, allowlisted `wp_options`, allowlisted single-row `wp_postmeta`, and exact positive-id `wp_reprint_push_forms_lab` rows use a single guarded `wpdb` update comparing expected stored columns at the SQL write boundary. Evidence is hash-only `storageGuard` with boundary, driver, logical/physical table, operation, compared columns, expected resource/storage hashes, rows affected, outcome, and SQL shape hash. | `npm run test:playground:storage-guarded-db-write` |
| Playground storage-boundary drift failures | Value drift on each supported table, marker-empty ownership drift for posts and postmeta parents, and absent/delete drift fail closed with rows affected `0`, outcome `stale-at-write`, `PRECONDITION_FAILED`, drift preserved, no `mutation-applied` for the failed target, no later mutations, and no `apply-committed`. | `npm run test:playground:storage-guarded-db-write` |
| Playground storage-boundary guarded fixture file writes | After the JIT pre-write hash passes, fixture upload file update/create mutations compare live file bytes/hash against the storage value observed after JIT, write planned content to a same-directory temp file, and rename after the comparison; fixture upload file deletes compare the same storage value before unlinking. Evidence is hash-only `storageGuard` with boundary `filesystem-compare-rename` for update/create or `filesystem-compare-unlink` for delete, driver, operation, logical fixture path, compared fields, expected resource/storage hashes, actual/planned storage hashes, physical path hash, and outcome `applied`. | `npm run test:playground:storage-guarded-file-write` |
| Playground storage-boundary file drift failures | Post-JIT/pre-write file drift on update, create, or delete returns `PRECONDITION_FAILED`, preserves the drifted file state, records no `mutation-applied` for the failed file, runs no later mutations, writes no `apply-committed`, replays same key/body with no fresh mutation work, and conflicts on same key/different body. | `npm run test:playground:storage-guarded-file-write` |
| Playground DB idempotency concurrent same-body first apply | A unique `claim_key_hash` opens exactly one claim before mutation; concurrent same-key/same-body first applies produce one fresh mutation executor and the duplicate returns safe in-progress/retry/replay behavior without mutation. | `npm run test:playground:db-journal-idempotency` |
| Playground DB idempotency concurrent different-body first apply | Concurrent same-key/different-body requests reject the conflicting request with `409 IDEMPOTENCY_KEY_CONFLICT` before mutation while the original request is the only fresh mutation executor. | `npm run test:playground:db-journal-idempotency` |
| Playground DB process-kill persistence | A real `SIGKILL` during an in-flight DB-journaled REST apply leaves persisted DB `idempotency-opened`/`apply-started` rows after host-mounted Playground restart without a false `apply-committed`. | `npm run test:playground:db-journal-process-kill` |
| Playground DB process-kill recovery block | After hard kill/restart, DB planned evidence plus live target hashes classify mixed old/new state, recovery inspection returns non-mutating `RECOVERY_BLOCKED`, and retry does not overwrite the partial state without relying on the legacy option journal. | `npm run test:playground:db-journal-process-kill` |
| Playground DB missing-commit finalization | A lab hook leaves all live target hashes at planned after hashes with DB mutation evidence but no `apply-committed`; same key/body returns `BATCH_RECOVERY_FINALIZED` with zero fresh mutation work, while same key/different body conflicts before finalization. | `npm run test:playground:db-journal-missing-commit-finalization` |
| Playground DB all-old stale-claim retry | A lab hook writes `idempotency-opened`, `apply-started`, and `stale-claim-abandoned` with no mutation or terminal rows; same key/different body conflicts, while exact same key/body retry requires matching abandonment evidence, validated started targets, zero mutation evidence, and all live target hashes at old values before appending derived `stale-claim-retry-started`, performing one fresh mutation set, committing, and replaying later as `BATCH_ALREADY_COMMITTED`. | `npm run test:playground:db-journal-stale-claim-all-old` |
| Playground DB stale retry guard and retry-start negative | If the derived stale retry claim already exists before retry `apply-started`/mutation, a later exact retry returns `IDEMPOTENCY_KEY_IN_PROGRESS` and does not mutate; if a retry `apply-started` exists without matching abandonment evidence, later retry blocks with `RECOVERY_BLOCKED` instead of reusing older abandonment evidence. | `npm run test:playground:db-journal-stale-claim-all-old` |
| Fixture plugin install atomic positive path | Base/remote lack fixture plugins; local includes dependency and dependent fixture plugins in one atomic group; apply activates both, writes exact fixture plugin files/resources and allowlisted option data, and replay does zero fresh mutation work. | `npm run test:playground:plugin-atomic-install` |
| Fixture plugin install dependency negatives | Missing dependency, dependency outside group, incompatible version, hash mismatch, activation requirement mismatch, remote dependency drift, and stale precondition reject before mutation or preserve the target. | `npm run test:playground:plugin-atomic-install` |
| Fixture plugin install forged ready plans | Forged ready plans omitting the dependency mutation, `atomicGroups`, dependency requirements, or live dependency evidence reject with executor-side validation before mutation. | `npm run test:playground:plugin-atomic-install` |
| Fixture plugin same-apply staged proof | Activation-style fixture plugin mutations may accept an inactive staged plugin hash only when `preWriteStagingProof` shows an earlier same-apply plugin file mutation covered by the declared ready atomic group. | `npm run test:playground:plugin-atomic-install` |
| Fixture plugin staged proof negatives | External staged plugin files without prior same-apply proof, forged mutation-local group ids without declared atomic-group coverage, and planned inactive plugin mutations all reject with `PRECONDITION_FAILED` before activation/commit. | `npm run test:playground:plugin-atomic-install` |
| Fixture plugin row-only plugin-owned data bypass | A row-only dependent plugin-owned option plan cannot bypass the atomic dependency requirement and rejects with `ATOMIC_GROUP_DEPENDENCY_UNDECLARED`. | `npm run test:playground:plugin-atomic-install` |
| Exact fixture plugin allowlist | The lab accepts only named fixture plugin files/resources, allowlisted plugin-owned options, and the exact forms lab custom-table driver; arbitrary plugin files, direct `active_plugins` row mutation, other custom-table apply, and arbitrary plugin-owned data remain blocked. | `npm run test:playground:plugin-atomic-install` / `npm run test:playground:forms-lab-table` / `scripts/playground/snapshot-lib.php` |
| Fixture plugin install failure injection | Before-commit failure preserves the old remote; during-publish and activation failures classify blocked recovery/no fresh retry mutation work instead of proving rollback. | `npm run test:playground:plugin-atomic-install` |
| Playground lab injected recovery failure | Fail-after-2 returns `LAB_INJECTED_APPLY_FAILURE` after two successful whole-resource mutations, records planned recovery entries and hash-only current state, and inspection reports `blocked-recovery` with `2 new` and `6 old` targets. | `npm run test:playground:recovery` |
| Playground lab retry after partial apply | Retry over the partial lab state refuses with `PRECONDITION_FAILED` instead of applying over the blocked recovery state. | `npm run test:playground:recovery` |
| File-backed JSONL recovery journal opens and restarts | Append-only JSONL records use monotonic sequences, include `fsync` evidence after each append, and can be read after restart-style inspection. | `npm run test:recovery:file-journal` |
| File-backed JSONL old remote before mutation | A journal opened before any mutation inspects as `old-remote` instead of pretending success. | `npm run test:recovery:file-journal` |
| File-backed JSONL partial apply recovery | Fail-after-2 inspects as `blocked-recovery` with `2 new`, `6 old`, and `0` unknown targets; retry refuses with `PRECONDITION_FAILED` and leaves the remote unchanged. | `npm run test:recovery:file-journal` |
| File-backed JSONL completed replay | A completed replay applies `0` additional mutations and inspects as fully updated or already committed. | `npm run test:recovery:file-journal` |
| File-backed JSONL drift outside recovery envelope | Current state outside journaled before/after hashes reports `blockedUnknown > 0` instead of reusing stale local data. | `npm run test:recovery:file-journal` |
| File-backed JSONL raw-value guard | Journal files contain no raw fixture fields/data; prevention is forbidden-key/fixture-string based, not a full production allowlist schema. | `npm run test:recovery:file-journal` |
| Failure happens while staging mutations | No partially mutated remote state is returned or committed. | `injected failure before commit returns no partially mutated remote state` |

## Remaining Missing Scenarios

- Production Reprint HTTP source mutation endpoint for live source sites.
- Reprint exporter protocol extension for authenticated push preflight,
  sessions, signed/expiring production receipts, and mutation batches.
- Production auth/session/nonce proof for any source-site REST mutation route;
  the authenticated Playground slice is local lab HMAC evidence only and does
  not prove production Reprint auth, TLS deployment, nonce/replay store cleanup,
  production session handling, production Application Password integration,
  real exporter credential binding, durable production audit records, or full
  production push.
- File body streaming with large upload chunks.
- Database transaction boundaries on MySQL and SQLite.
- Production storage-level compare-and-swap or locking around final target
  writes. The current JIT pre-write hash check plus
  `npm run test:playground:storage-guarded-db-write` and
  `npm run test:playground:storage-guarded-file-write` proves only local
  Playground fixture slices: update-only guarded SQL for existing `wp_posts`,
  allowlisted `wp_options`, single-row `wp_postmeta`, exact
  `wp_reprint_push_forms_lab` rows, and guarded compare/rename/unlink for
  fixture upload file update/create/delete. The file code path supports named
  fixture plugin file update paths, but the standalone smoke does not exercise
  fixture plugin-file writes. These are not generic MySQL/InnoDB or filesystem
  CAS, transactions/locking, rollback, storage `fsync`, arbitrary file
  guarding, plugin activation guarding, or production Reprint HTTP mutation.
- Production plugin activation/update with dependency and recovery checks.
- Object-cache, cron, generated files, and maintenance-mode interactions.
- Plugin validator and merge-driver contracts with real plugin fixtures; the
  current forms slice, exact forms lab custom-table driver, and fixture plugin install atomicity slice are
  fixture/allowlist-scoped and do not prove arbitrary production plugin-owned
  options, postmeta, custom tables, production plugin installation/update,
  production activation semantics, or production rollback.
- General WordPress graph identity mapping and reference rewriting. The current
  planner proof blocks a local `wp_postmeta.post_id` reference when the target
  `wp_posts` identity changed on the remote since the pull base, and it records
  hash-only relationship evidence. The planner also refuses reference-bearing
  rows whose targets are absent from the live remote, so same-plan graph
  identity creates stay blocked. It does not prove safe automatic rewriting for
  attachments, GUIDs, nav menus, term splitting, serialized blocks,
  `_thumbnail_id`, `post_parent`, `wp_term_relationships`,
  `wp_term_taxonomy`, `wp_termmeta`, cross-table create batches, or production
  importer/exporter identity maps.
- Production DB-table journal and kill-process recovery tests around every
  durable WordPress boundary. The current DB journal/idempotency/process-kill
  plus missing-commit finalization and all-old stale-claim retry slices are
  fixture-scoped local Playground SQLite/host-mount evidence only, not
  production durability, storage `fsync`, rollback, exactly-once production
  writes, arbitrary plugin data safety, or full MySQL/InnoDB behavior. The
  stale-claim slice does not solve production stale-claim leases, fencing,
  claim expiry, cross-process/shared-DB lock proof, arbitrary production
  repair, or production retry policy; observed-hash assertions are still
  shallow in places, and production auth/live source mutation/full push remains
  pending. The JSONL journal has per-append `fsync` evidence in the JSON-model
  lab, and the Playground recovery harness is injected lab failure inspection
  only.

## Invariant Policy

The planner policy is documented in
[No Overwrite Invariants](invariants/no-overwrite.md).
