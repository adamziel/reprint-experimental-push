Performance model hang diagnosis

Narrowed command:
```bash
timeout 20s node --test --test-name-pattern='guarded executor large profile still preserves receipts and stays blocked for production throughput' test/performance-model.test.js
```

Result:
- The command printed only `TAP version 13` and then timed out with exit code `124`.

Comparison commands that completed:
- `timeout 20s node --test --test-name-pattern='benchmark model covers large uploads and plugin installs' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='safety contract covers required speedup areas and terminal states' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='fast-path proofs and rejections carry the expected gate metadata' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='fast-path fixture isolates the release-safety benchmark shape' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='file hashing and compression decisions preserve canonical hashes' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='chunk uploads stay staged until a guarded publish step' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='database batching is bounded and keeps per-row preconditions' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='failure boundaries carry the receipts and commit records needed for recovery' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='plugin install remains invisible until the atomic group commit' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='plugin update keeps staged work behind the same recovery barriers' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='parallelism limits and backpressure budgets are explicit' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='rejected fast paths cover precondition bypasses and atomic group splits' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='safe fast paths retain all gate proofs and stay non-rejectable' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='failure injection boundaries include every durable transition in the benchmark shape' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='production throughput stays blocked until measured storage receipts exist' test/performance-model.test.js`
- `timeout 20s node --test --test-name-pattern='guarded executor report keeps the large-upload and plugin-install evidence visible' test/performance-model.test.js`

Assessment:
- The full-file timeout appears pre-existing rather than introduced by the rejected fast-path coverage in this lane.
- The isolated hang is in the `guardedLarge` benchmark path, not in the earlier fast-path model tests.
