Date: 2026-05-25

Focused command:
`timeout 15s node --test --test-name-pattern='production throughput stays blocked until measured storage receipts exist' test/performance-model.test.js`

Observed result:
The focused test passed in about 0.5s.

Full-file command:
`timeout 60s node --test test/performance-model.test.js`

Observed result:
The full file completed successfully in about 10.2s with 17 passing tests.

Diagnosis:
The earlier `TAP version 13` stall is no longer reproducible in this run. The guarded-executor path now completes under the test runner, so the prior timeout looks bounded or already resolved rather than a live hang in this lane.

Lane assessment:
The timeout is currently not reproducible. `node --test test/performance-model.test.js` can now complete.

Status:
Keep the note as the record of the earlier timeout, but do not add new fast-path cases until the supervisor redirects.
