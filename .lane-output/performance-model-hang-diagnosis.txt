Narrowed command:
`time timeout 90s node --test --test-name-pattern='guarded executor large profile still preserves receipts and stays blocked for production throughput' test/performance-model.test.js`

Result:
Passed in 8.947s real time with TAP output completing normally.

Conclusion:
The full-file `timeout 60s node --test test/performance-model.test.js` timeout is pre-existing cumulative runtime pressure, not a single hanging subtest introduced by the lane. The isolated large-profile test completes on its own, so the file-wide 60s ceiling can be exceeded by aggregate runtime even when individual tests pass.
